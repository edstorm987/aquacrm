{
  "name": "@aqua/plugin-leads-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Aqua portal leads-pipeline plugin \u2014 CSV contact import, single-shot email campaigns and lead/contact domain. Anonymous Public Funnel captures remain pending outside CRM; only an explicit mailbox-proven or authenticated promotion bridge creates exact Lead, Person, Prospect and pipeline-card lineage.",
  "main": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./manifest": "./index.ts",
    "./server": "./src/server/index.ts",
    "./types": "./src/lib/aquaPluginTypes.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "smoke": "tsx --test src/__smoke__/leads-pipeline.test.ts",
    "test": "tsx --test src/__smoke__/leads-pipeline.test.ts"
  },
  "peerDependencies": {
    "next": "^16.0.0",
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
