import { PortalViewportLoading } from "@/components/ui/PortalViewportLoading";

// This segment covers every Agency child route, not just the home screen. Keep
// the message and visual deliberately workspace-neutral.
export default function AgencyLoading() {
  return <PortalViewportLoading />;
}
