# `src/app/portal/agency/radar/page.tsx`

← [File index](../../../../../../files-index.md) · Area: App routes & UI — src/app/

_No file-level doc-comment. Purpose inferred from its path (App routes & UI — src/app/) and its exports below._

## Exports (1)

- `default async RadarInspectionPage({ searchParams }: { searchParams: Promise<{ view?: string; query?: string; domain?: string; status?: string; scope?: string; lens?: string; source?: string; dataset?: string }> })`

## Used by

_No internal importers found (an entry point — route/page/test/script — or dynamically loaded)._

