# `src/app/portal/clients/[clientId]/_ToolsPicker.tsx`

← [File index](../../../../../../files-index.md) · Area: App routes & UI — src/app/

_No file-level doc-comment. Purpose inferred from its path (App routes & UI — src/app/) and its exports below._

## Exports (2)

- `interface PickerPlugin (6 members)`
- `ToolsPicker({ clientId, plugins, isLive = false, liveRecommended = [], }: { clientId: string; plugins: PickerPlugin[]; isLive?: boolean; liveRecommended?: readonly string[]; })`

## Depends on (1)

- [`src/app/portal/clients/[clientId]/toolCopy.ts`](./toolCopy.md)

## Used by

_No internal importers found (an entry point — route/page/test/script — or dynamically loaded)._

