# `src/lib/healthCheck/defaultPack.ts`

← [File index](../../../../files-index.md) · Area: Shared logic — src/lib/

**What it is:** T4 R008 — default HC question pack, hand-ported from `public/health-check/hc-questions.js` (the static app's source of truth). Once parity is signed off, the static JS file retires and this module becomes the canonical pack. Per-niche-agency packs (Phase 12 R3) will return objects of this same shape.

## Exports (1)

- `defaultPack: HCPack`

## Depends on (1)

- [`src/lib/healthCheck/types.ts`](./types.md)

## Used by

_No internal importers found (an entry point — route/page/test/script — or dynamically loaded)._

