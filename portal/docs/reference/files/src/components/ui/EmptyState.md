# `src/components/ui/EmptyState.tsx`

← [File index](../../../../files-index.md) · Area: Components — src/components/

**What it is:** EmptyState — friendly placeholder for "no rows yet" lists. Replaces the bare empty `<ul>` / "0 items" pattern across the 25+ list pages flagged in the audit. Accepts a heading, body, optional icon glyph, and a single primary CTA (link or button).  Visual: dashed border + subtle bg, centred. Brand-neutral so it inherits the tenant's brand tokens via globals.css.

## Exports (2)

- `interface EmptyStateProps (6 members)`
- `EmptyState({ heading, body, icon, cta, secondaryCta, className }: EmptyStateProps)`

## Used by

_No internal importers found (an entry point — route/page/test/script — or dynamically loaded)._

