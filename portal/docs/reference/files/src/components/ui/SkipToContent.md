# `src/components/ui/SkipToContent.tsx`

← [File index](../../../../files-index.md) · Area: Components — src/components/

**What it is:** SkipToContent — first interactive element in the document, hidden off-screen until focused. Lets keyboard users jump past the chrome straight to `<main>`. Mounted at the root layout level.

## Exports (1)

- `SkipToContent({ targetId = "main-content" }: { targetId?: string })`

## Used by (1)

- [`src/app/layout.tsx`](../../app/layout.md)

