# `scripts/seed-dev-tenant.ts`

← [File index](../../files-index.md) · Area: Scripts — scripts/

**What it is:** Seeds the `Bare Co` dev tenant with contact/company fixtures covering every card state, so dev mode always has something meaningful to drive.  Idempotent — re-run freely. Safe by construction: refuses to run against a durable backend, the same guard dev mode itself uses.  PORTAL_BACKEND=file npx tsx scripts/seed-dev-tenant.ts

_No exported symbols (side-effect / internal module)._

## Used by

_No internal importers found (an entry point — route/page/test/script — or dynamically loaded)._

