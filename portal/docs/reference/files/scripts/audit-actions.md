# `scripts/audit-actions.ts`

← [File index](../../files-index.md) · Area: Scripts — scripts/

**What it is:** What state Actions is actually in — measured, not remembered. For every alert the checks currently raise: is it classified, does it carry a destination and a focus, does it have a resolution plan, and is its evidence built from the real records or the generic fallback? NODE_OPTIONS='--conditions react-server' npx tsx scripts/audit-actions.ts Reads the local `.data` sandbox. Read-only — it raises alerts and asks for their plans, it does not accept, resolve or write anything.

_No exported symbols (side-effect / internal module)._

## Used by

_No internal importers found (an entry point — route/page/test/script — or dynamically loaded)._

