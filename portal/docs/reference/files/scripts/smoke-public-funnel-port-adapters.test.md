# `scripts/smoke-public-funnel-port-adapters.test.ts`

← [File index](../../files-index.md) · Area: Scripts — scripts/

**What it is:** T1 R032 smoke — public-funnel + BOS port adapters + dispatcher public:true. Run via `npm run smoke:public-funnel-port-adapters` (tsx --test).  All load-bearing wire-up is behind server-only — leadFunnelPorts + _types + dispatcher route handler. Source-marker covers the shipped contract.

_No exported symbols (side-effect / internal module)._

## Used by

_No internal importers found (an entry point — route/page/test/script — or dynamically loaded)._

