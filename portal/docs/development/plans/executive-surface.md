# Executive surface — launcher vs move-lite (queue #10)

**Status:** PLAN — needs Ed's shape call (A launcher vs C move-lite, plus the Day-briefing and Capital/Ownership placement questions). Captured 2026-08-21, autonomous loop, from code investigation.

## Proven current state

- **IA v2 plan**: `docs/development/plans/information-architecture-v2.md:16-24` defines the five surfaces; `:33` maps "Battle Table · Radar · Command Intelligence · Capital/Ownership · Day briefing → Executive (pulled out of Command Centre stations)"; Phase 1 (`:62-63`) says "move the strategic stations … into it; Command Centre keeps the daily view". Phase 2 (Operations container) is marked DONE 2026-08-21 (`:64-70`). Open decisions for Ed at `:75-79` (Finance/Capital split, You-deserve-it home, owner-only vs scoped Executive).
- **Sidebar mounts NOTHING for Executive today**: `src/lib/chrome/sidebarLayout.ts:259` — agency override renders only `["home", "inbox", "operations-home", "tools"]`; the comment at `:248-251` says explicitly "(Executive arrives in a later lane.)". Operations functions live in a hidden search-only `ops` panel (`:263-303`, `NavPanel.hidden` at `:27-33`).
- **Strategic instruments live inside the Command Centre monolith** (`src/app/portal/agency/_DashboardCommandCenter.tsx`, 2,585 lines):
  - Stations are `CommandStationMode = "executive" | "day" | "battle" | "devteam"` (`_CommandStationNav.tsx:5`), rendered as buttons at `:56-59` ("Day command", "Command Centre" unified watch, "Battle Table", "Dev Console").
  - Station switch/render at `_DashboardCommandCenter.tsx:1059`: `executive` renders the `executiveWorkspace` prop + `CommandInstrumentDock` + `CommandCentreKpiTrajectory`; `battle` renders `BattleTableWorkspace`; else day/intelligence/radar.
  - `executiveWorkspace` is a ~160-line server-rendered HUD built inline in `src/app/portal/agency/page.tsx:239-400` (telemetry cards, HudDials, DynamicRadarConsole, BrandPortfolioInstrument, SIGINT feed) and passed at `page.tsx:445`; typed as `React.ReactNode` at `_DashboardCommandCenter.tsx:200`.
  - **Capital/Ownership** is a Battle Table section: `_BattleTableWorkspace.tsx:56` (`BattleTableSection` includes `"capital"`, `"reviews"`, `"strategy"`, `"projections"`…), mounting `CapitalOwnershipWorkspace` at `:254` and `QuarterlyStrategyReview` at `:635`.
  - **Command Intelligence** = `_CommandIntelligenceWorkspace.tsx` (1,026 lines), **Radar inspector** = `radar/RadarInspectionWorkspace.tsx` (1,008 lines); all three heavy stations already code-split via `next/dynamic` (`_DashboardCommandCenter.tsx:82-105`).
  - **Day briefing** = `_DayBriefingPanel.tsx` imported at `_DashboardCommandCenter.tsx:78` (day station — the plan keeps daily in CC, note the `:33` table lists it under Executive: a genuine ambiguity for Ed).
- **Deep links to every instrument already exist**: `?station=` parsing at `_DashboardCommandCenter.tsx:2427-2435` (`executive|day|battle|intelligence|radar`, aliases `omega`, `radar-inspector`); `?battle=capital|strategy|projections|reviews…` at `:2480-2488`; `?view=` intelligence views at `:2299-2302`. Route aliases exist: `command-center/page.tsx:1` is literally `export { default } from "../page"`, and `radar/page.tsx:3-11` redirects to `/portal/agency?station=radar-inspector` preserving params.
- **The launcher pattern is already built once**: Operations hub `src/app/portal/agency/operations/page.tsx` (207 lines) is a pure Link-card launcher, kept in lock-step with the sidebar by `scripts/smoke-operations-hub.test.ts` (comment `:26-29`); single-row active-state + attention rollup in `src/components/chrome/SidebarNavLink.tsx:183-202` (`OPERATIONS_ACTIVE_PREFIXES`, `OPERATIONS_ATTENTION_DESTINATIONS`) and `:310-327`; guards `scripts/smoke-operations-surface.test.ts`, `smoke-operations-attention-rollup.test.ts`, `smoke-nav-audit.test.ts`.
- **Perf coupling**: the agency page builds radar + KPI intelligence server-side for all stations (`page.tsx:41-43,97-104` — `shouldRunHeavyPanels`, `buildPausedBusinessRadar`; `scanPaused` prop at `_DashboardCommandCenter.tsx:214-216`), so strategic payloads ride every Command Centre load today (unless perf-paused).
## What is genuinely missing

- Any `/portal/agency/executive` route (no `executive/` dir under `src/app/portal/agency/`).
- Any Executive row in the sidebar (`sidebarLayout.ts:259` omits it by design, "later lane").
- A decision on the Battle-Table split: Capital/Ownership and Quarterly reviews are *sections inside* Battle Table, not standalone routes — "move Capital/Ownership to Executive" has no standalone artifact to move yet.
- Resolution of the plan's internal ambiguity: Day briefing listed under Executive at `information-architecture-v2.md:33` but kept in CC by `:57` and `:63`.
## Options

**Option A — Launcher (Executive = hub of cards over existing stations).** Clone the Operations-hub pattern: new `executive/page.tsx` with cards linking to `/portal/agency?station=battle`, `?station=intelligence`, `?station=radar`, `?station=battle&battle=capital`, plus an `executive` sidebar row in `commandCentreIds` (`sidebarLayout.ts:259`) and rollup/active-prefix entries in `SidebarNavLink.tsx`. Zero station code moves; CC keeps all stations. **Cost**: ~1 new page + ~10 lines of chrome + 2-3 smoke tests (copy the operations trio). Days, not weeks. **Risk**: low — but Executive is then a bounce-through page (two clicks to Battle Table), CC stays crowded (contradicts `:57` "v2 splits them"), and CC keeps paying the heavy radar/intelligence server builds on every daily load.

**Option B — Move (Executive = own surface hosting the strategic stations).** New `/portal/agency/executive` route re-exporting `../page` (the proven `command-center/page.tsx:1` trick), with `initialStation` derived from pathname (`_DashboardCommandCenter.tsx:236,245`) and `CommandStationNav` filtered per surface: CC shows day (+devteam), Executive shows executive-watch/battle/intelligence/radar. Old `?station=` deep links keep resolving (redirect or accept both). **Cost**: moderate — edits inside the 2,585-line `_DashboardCommandCenter.tsx` (initialStation `:245`, `commandStationMode` `:2427`, station nav `:55-59`, attention record `:1026`), sidebar row, several smoke updates. **Risk**: medium — the monolith still serves both routes so CC still assembles strategic payloads server-side (perf win requires also gating `battleTablePayload`/`executiveWorkspace` building in `page.tsx:220-445` by route, which is real surgery); regression surface is the founder's daily home.

**Option C — Move-lite / hybrid (recommended).** Option B's route + station split, but Executive's landing IS the existing `executiveWorkspace` HUD (already a self-contained ReactNode built at `page.tsx:239-400`) with the Battle/Intelligence/Radar stations beneath it — i.e. the "unified executive watch" station becomes the Executive front door, CC's station nav drops to Day (+Dev). Defer splitting Capital/Ownership out of Battle Table (it stays a `?battle=capital` deep link, card on Executive). **Cost**: Option B's chrome work without new page assembly; no component relocations at all. **Risk**: medium-low; perf gating of heavy payloads on the CC route can land as a follow-up using the existing `commandPerformance.ts` pause machinery.
## Recommendation

Option C. The plan's own reconciliation (`information-architecture-v2.md:57`) says v2 *splits* daily from strategic — a pure launcher (A) doesn't honour that and leaves CC crowded; a full extraction (B) rewrites a 2,585-line monolith for no user-visible gain over C. C reuses every existing artifact (route re-export trick, station machinery, deep links, Operations chrome pattern + its three smoke tests) and leaves the Capital/Ownership and Finance-split questions open exactly where Ed's plan says they're open (`:75-77`). But this is queue #10 *needs Ed's shape call* — present A vs C; A is the cheap fallback if Ed wants CC untouched.
## Risks

- CC is Ed's daily home; any station-nav change regresses muscle memory and the `?station=` deep links scattered through docs/tests — the alias table at `_DashboardCommandCenter.tsx:2427-2435` must keep resolving.
- Perf: whichever shape, the heavy radar/intelligence builds (`page.tsx:97-104`) stay on the CC critical path until route-gated — don't promise a faster CC from the IA move alone.
- Plan ambiguity (Day briefing in `:33` vs `:57`) and the Finance/Capital split (`:76`) must be settled by Ed before the sidebar row ships, or the taxonomy churns twice.
- `smoke-nav-audit` / operations-surface guards pin the current four-row sidebar; they need updating in the same change, not after.
## Phases

1. **Shape call** — put Option A (launcher) vs Option C (move-lite) to Ed, plus the Day-briefing and Capital/Ownership placement questions from information-architecture-v2.md:75-79; PLAN ONLY until he answers.
2. **Executive route + sidebar row** — add `executive/page.tsx` (re-export or hub card page per Ed's call) and an `executive` entry in `commandCentreIds` (sidebarLayout.ts:259) with active-prefix + rollup wiring in SidebarNavLink.tsx.
3. **Station split (C only)** — filter CommandStationNav per surface (day+dev on CC; watch/battle/intelligence/radar on Executive), keep every `?station=`/`?battle=` alias resolving via redirect.
4. **Guards** — clone the operations smoke trio into smoke-executive-surface/-hub tests and update smoke-nav-audit for the five-row sidebar.
5. **Perf follow-up** — route-gate `battleTablePayload`/radar/intelligence assembly in page.tsx via commandPerformance.ts so the CC daily view stops paying Executive's build cost.
6. **Docs** — mark IA v2 Phase 1 done, update sidebarLayout comments, workspace chapter, and feature-index per docs/development.md law.
