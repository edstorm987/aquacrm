# Codex status

## 2026-09-12 current checkpoint

- Worktree: `/Users/eds/Desktop/Projects/Web Development/Personal EcoSystem/aquaCRM-sales-workflow-recovered-20260912`
- Branch: `integration/full-build-20260912`, based on immutable recovery checkpoint `checkpoint/full-build-20260912-112701` at `3abd629ec87d43e4d5138af9204060df73e92bbc`.
- Preservation state: the frozen 282-file snapshot is committed with no conflict markers, no large/symlink/generated/local-state files, and no detected private-key or provider-token additions. GitHub independently reports exact SHA `3abd629ec87d43e4d5138af9204060df73e92bbc` for `checkpoint/full-build-20260912-112701`; ten scanned worker histories are also present under `checkpoint/workers/*`, including route inventory `e3d1df1c`. Rejected SEC candidate `checkpoint/integration/full-build-20260912` is frozen at `2439e1b5`; the new accepted-only line `checkpoint/integration/accepted-full-build-20260912` is independently verified at `43aa898d541fc9174089e54e3a9dab60de42c4f8`. A verified 45 MB all-refs Git bundle remains as a second local copy. Remote `main` is independently confirmed unchanged at `d932ce665d9f6afaffa75ed7796a69b17ac7ee58`.
- Combined acquisition security/identity suite: 116/116 passed.
- `SEC-001` anonymous Health Check session takeover is closed locally and independently rerun: 40/40 focused checks passed. It is not deployed.
- `SEC-002` arbitrary-client signup/magic/setup/password-login takeover is closed locally: independent 13/13 exploit checks, full typecheck, and diff check passed. It is not deployed; pre-existing unbound client Supabase subjects need an explicit migration before release.
- `SEC-004` public-enquiry client assignment is closed locally: public email/phone/company/derived-lead evidence remains review-only, configured routes/manual review/typed lineage carry explicit provenance, and Inbox lead/contact links are tenant-existence checked with canonical-Person conflict refusal. Focused 42/42, full typecheck, and diff check passed. It is not deployed.
- `SEC-005` client enquiry disclosure is closed locally: customer and internal client records now require an exact client id plus recognised server-written provenance; public email/phone equality cannot disclose another submitter's message. Focused 60/60 passed. It is not deployed.
- `SEC-007` Sales/Contacts leaf access is closed locally: Journey, Contacts, Person edits/interactions, identity review, and enquiry-to-Sales bridges now enforce their exact Growth, Inbox, and linked-client Communications element floors. Person timelines use exact stored lineage only. Focused 59/59 passed. It is not deployed.
- `SEC-003` is locally complete after multiple independent stop-gates were corrected: exact shared/legacy identity preservation, cross-client funnel and brand-enquiry A/B cases, direct retry/rollback/idempotency, and the real inbox identity→conversation→message schema. Live inbox deletion is one service-role-only atomic RPC in local unapplied migration `20260912130000_atomic_client_inbox_erasure.sql`; 220/220 focused checks, full typecheck and diff check pass. Staging concurrency and migration-before-server order remain release gates.
- Residual exact-lineage work still open: `SEC-006` is in a fourth isolated corrective pass after independent rejection exposed unsound phone/nested ownership, unsafe string projection, silent evidence omissions, non-idempotent delivery, incomplete/non-local work bounds and missing signed CSRF. Brand-enquiry erasure/CAS and durable review/backfill follow-ups also remain open.
- `AUTH-001` is integrated in the recovered tree locally. Server verification and all discovered AquaCRM-owned callers now share exact `login`, `public-contact`, or `brand-enquiry` actions; cross-origin branded login binds the selected brand's exact Origin through a server-internal hostname argument; tokenless bots cannot spend victim contact quotas; client widget/script failures time out visibly. Combined local acceptance: 68/68, full `tsc --noEmit` green, `git diff --check` green. Production keys, provider hostname configuration, and Milesymedia's separate custom-login verifier remain parked; nothing was deployed or queried live.
- The screenshot-requested sign-in redesign and inherited contrast correction are independently accepted on the isolated login chain ending at `d804243305c19d57e3ded6d2573593b11b24bc40`; its GitHub worker checkpoint matches that exact SHA. Combined-tree integration and regression remain pending, so this is not yet a `main` claim.
- A fresh read-only public-abuse audit found eight additional launch-blocking admission families beyond the three newly protected routes. These are now explicit P0/P1 queue items rather than hidden risk: self-service owner signup, reset/magic mail abuse, alternate funnel routes, Aqua Tag/website-editor poisoning, careers quarantine, checkout/free-order abuse, embed cross-tenant authority, and durable multi-instance throttling. No production-readiness claim is made while they remain READY.
- Primary `main` remains untouched.
- Clean integrated local checkpoint: `35ceed09d04f707b6ecdbe2ff885fc8161172526`; recovered integration tree is clean. Separate Claude, ABUSE-001, and ABUSE-002 worktrees all start at that exact SHA.
- Fresh clean-checkpoint gates: Website Editor **49/49 files passed in 13.8s**, full TypeScript `tsc --noEmit` exited 0, and the isolated Next.js/Webpack production build compiled, typechecked, and generated **247/247** static pages. The tree remained clean. These are local scoped evidence only.
- `ABUSE-001` and `ABUSE-002` are active on disjoint isolated Codex branches. The Claude hardening tree is clean and reserved for a separate queue item selected by the under-4,000-character loop prompt.
- Ed authorises GitHub preservation checkpoints to `https://github.com/edstorm987/aquacrm.git` and eventual safe `main` promotion after combined acceptance. The destination is confirmed and current checkpoint SHAs have been independently verified with `git ls-remote`. Railway, Supabase cloud, DNS, providers, keys, migrations, production data, email, SMS, payments and live deployment mutation remain out of scope.
- The first ABUSE-001/002/003 combined tree passed 246/246 plus full typecheck, but independent source review correctly rejected completion. Active corrections now cover: Aqua Tag human proof/signing oracle, exact retry charging, full emitted-key/host registry, non-hardcoded telemetry host binding, Health Check email namespace squatting, auth-link canonical origins, signup verification replay, reset purpose/epoch/exact-subject/provider-local convergence, magic delivery/session revision, and caller-selected website-lead tenant routing. The integration tree remains uncommitted until these are closed and independently rerun.
- The first Health Check corrective follow-up `41e1de51` also failed independent acceptance despite 100/100 focused checks: the mounted anonymous route can bootstrap a founder on fresh state, global pending-email uniqueness is not enforced across installs, and automation-run event derivatives retain email/pending IDs after exact erasure. `ABUSE-002` and `HEALTH-ID-001` are ACTIVE again; corrective work is isolated and local-only.
- Aqua Tag corrective commit `072987f2` is integrated source-level and its 23 files match the isolated worker commit exactly. Worker evidence is 116/116 admission/telemetry/RLS/routes, 144/144 SEC/role/lineage, and 6/6 real loopback multi-process collision/recovery; typecheck/diff check were green. A fresh independent integrated-tree review is active, the additive migration remains unapplied, and no deployed-readiness claim is made.
- Health Check second corrective commit `96a003de` is integrated source-level and all seven files match the isolated commit. Public founder seeding is removed, pending-email uniqueness scans all public-funnel installs inside the global state transaction, and durable automation-run fields exclude email/pending ID/full answers. Worker matrices 134/134 + 164/164, typecheck/diff green; a second independent acceptance review is active.
- The invitation harness now saves/sets/restores its canonical loopback portal origin and passes 13/13 without external configuration. Independent auth review nevertheless found additional source-level P1s, so `ABUSE-001` is ACTIVE and not green.
- Independent auth acceptance formally rejected `a967871f` despite 77/77 focused checks and the 13/13 invitation harness. Six P1 families are now explicit and ACTIVE: Supabase production falling back to process-local nonces; broken scoped customer reset issuance/same-email privileged selection; pre-reset magic tokens surviving session-epoch rotation; agency-signup retry password split-brain; invitation/client-setup provider and persistence windows; and known-account delivery exceptions exposing an oracle. The same isolated auth lane is correcting them locally.
- Independent Aqua Tag acceptance also rejected `072987f2` despite 112/112 focused checks, six loopback process cases, clean typecheck and diff. Remaining P1s are exact `www` challenge-host handling, telemetry persistence ignoring the already-resolved tenant/client scope, and ambiguous PostgREST status-0 errors being treated as proven rollback/refund; legacy migration adoption also falsifies exact replay/attached receipt semantics. The isolated tag lane is correcting these locally.
- Second Health Check acceptance rejected `96a003de`: its pending row and automation-run sanitization are sound, but the raw plugin subscriber path immediately persists submitted email into Lead, acquisition profile, email index, Prospect, Person and pipeline-card rows plus a `person.created` outbox event; exact capture erasure leaves every derivative. The correction now enforces pending-only anonymous capture and a separate idempotent verified/authenticated promotion boundary.
- `FILE-SEC-001` is locally implemented and awaiting independent review. Careers now uses exact human proof before victim-email/file work and never seeds founder state; complete artifacts go through a credentialed redirect-closed AV/CDR gateway; scanner absence/outage/legacy metadata stays quarantined; operator download is fail-closed and attachment-only; authorised managers can retry scanning after recovery. Focused 55/55 plus environment/readiness 42/42 and full typecheck are green. Production scanner credentials/staging behavior remain an explicit human release gate; nothing external was contacted.
- Aqua Tag third follow-up `aff2aaec` is now source-integrated. Its 13 source/test/migration files match the isolated worker exactly; shared docs preserve both Tag and FILE-SEC truth. Worker evidence is 121/121 focused, 144/144 role/lineage, 6/6 loopback multi-process, tsc/diff green. Exact challenge-host separation, resolved telemetry scope, ambiguous status-0 no-refund reconciliation, and truthful legacy adoption are implemented; independent re-review is active. The additive migration remains unapplied and no live/cloud/network action occurred.
- Health Check third follow-up `b7140418` is now source-integrated and all 24 files match its clean isolated commit. Anonymous completion is pending-only and creates no auth/CRM/email-index/card/outbox identity derivative; a separate exact-proof/authenticated idempotent promotion bridge carries capture lineage. Worker evidence is 122/122 focused, 208/208 broad, 128/128 auth/session, 19/19 loopback, tsc/diff green. Independent re-review is active; no public mailbox-proof endpoint was invented and durable cross-process controls remain under ABUSE-BASE-001.
- Aqua Tag `aff2aaec` did not pass independent re-review. Duplicate agency master keys still choose the first tenant before the admission candidate-uniqueness check, and durable consent events omit resolved tenant/client/site lineage. The four earlier rejection families are verified corrected, but ABUSE-003/TAG-KEYS/TELEMETRY remain ACTIVE and the same isolated lane is correcting these two residuals locally.
- Health Check `b7140418` did not pass independent re-review. Anonymous capture is now genuinely pending-only, but the promotion command accepts forged actor/proof strings and exact erasure/retention leaves a capture-linked automation run. ABUSE-002/HEALTH-ID/HEALTH-PROMOTE remain ACTIVE; the same isolated lane is replacing claimed authority with server-validated authority and closing derivative cleanup.
- Auth correction `cd637e46` is source-integrated and in fresh independent acceptance. The Supabase nonce migration was renumbered to `20260912150000` to avoid a duplicate migration version with Aqua Tag; it remains unapplied. Local invitation/nonce/MFA loopback is 47/47, and the Tag/schema drift exposed during integration is corrected 27/27. Nothing live was contacted.
- Aqua Tag correction `7edf1238` is source-integrated and awaits fresh independent acceptance. Duplicate master-key/host ownership now fails closed and consent audit rows keep immutable resolved tenancy/site lineage. The Dev API page truthfully lists the challenge-config endpoint and the schema inventory counts the new nonce table.
- `COMMERCE-SEC-001` is locally implemented and in VERIFY: public checkout is exact-host/proof/total-class bound, budgets durable multidimensional limits before checkout work, and separates paid from zero-value proof. Adjacent checkout/order/Website Editor suites are 48/48 and combined TypeScript is green. Real provider staging remains explicitly external and untouched.
- Independent auth re-review rejected `cd637e46`: provider recovery authority still uses subject-editable Supabase metadata, signup response timing distinguishes existing from fresh addresses, and ambiguous magic/reset delivery can mint multiple live bearers. ABUSE-001 and the affected auth items are ACTIVE again in an isolated correction lane; the nonce, exact-audience, epoch, client-setup and consent controls remain verified.
- Health correction `c197af25` is source-integrated and in fresh independent acceptance. Promotion authority is now verified by the host from an opaque fresh agency-session credential; mailbox proof fails closed until a durable receipt exists. Exact capture/activity/ledger/automation lineage erasure and retention pass the integrated 157/157 matrix.
- Independent FILE acceptance rejected the first green implementation: multipart bytes were consumed before bot proof, rescan could diverge in-memory from failed durable persistence, served bytes were not re-hashed against the clean verdict, and scanner SSRF controls lacked DNS pinning/exact configured-origin enforcement. `FILE-SEC-001` is ACTIVE in a local-only correction lane, including CSRF, provider-cost/concurrency and durable audit follow-ups. Production scanner configuration remains human-blocked and untouched.
- Public plugin tenancy is no longer an unbounded audit item: all 15 anonymous plugin routes are classified in `PUBLIC-PLUGIN-TENANT-INVENTORY.md`. `PUBLIC-PLUGIN-TENANT-001` is ACTIVE; the next local slice is a fail-closed authority registry plus exact provider-event/install and registered-storefront-host binding. This inventory is routing evidence, not acceptance or deployed readiness.
- Health correction `c197af25` failed its fourth independent acceptance despite its green focused suites. Revocation during lock wait, cross-capture credential replay, and the missing promoted-to-CRM erasure bridge keep `ABUSE-002`, `HEALTH-PROMOTE-001`, and `HEALTH-ID-001` ACTIVE. A fifth disjoint local correction lane is running; no production-readiness claim is made.
- Public plugin authority is now fail-closed at registration locally: all 15 anonymous routes declare a typed authority class, and unclassified/invented classes are refused by the dispatcher. Every storefront read/quote/receipt facade also binds back to one exact registered host before storage work. Local tenancy/registry **32/32** and commerce-adjacent **52/52** are green; provider webhook ownership and fresh independent acceptance remain open.
- All six anonymous provider webhook facades now have a shared 256 KiB streamed body boundary and verify their signature/capability before consuming bytes. Finance, Memberships, Ecommerce, and Leads have exact signed tenant-scope checks locally; the 76/76 provider/module matrix and 23/23 close-deal regression are green. Affiliates cross-install provider ownership and Postmark query-secret transport remain open, so this is not an acceptance or launch claim.
- Every public Ecommerce storefront facade now binds to one exact registered published host before storage work, and quote/checkout JSON is capped at 64 KiB even when chunked. Commerce/Website Editor adjacency is 52/52 and the focused public checkout/order registry subset is 28/28. Fresh independent runtime/CORS/provider review remains required; nothing live was contacted.
- A graph-documented Affiliates Stripe defect is corrected locally: the Connect callback no longer reuses Ecommerce's different endpoint secret, and automated payout readiness requires the Affiliates-specific vault secret. Postmark no longer carries secrets in query strings and now proves exact local MessageID ownership before an atomic claim. The relevant local matrices are 123/123 and 28/28; both still require fresh independent acceptance and provider staging.
- The FILE correction worker is complete locally with 113/113 focused tests: proof/size-before-body, pinned scanner egress, transactional quarantine/release, served-byte digest checks, CSRF/scope/budget/audit controls. A fresh independent acceptance reviewer is active; real scanner provisioning and staging remain external human gates.
- The fifth Health correction is complete locally with 115/115 focused tests plus clean TypeScript/diff: authority is revalidated under the durable lock, claims are exact-capture bound, and promoted/full-client erasure uses the real atomic CRM graph bridge with rollback and shared-identity preservation. It is not green until a fresh independent reviewer accepts it.
- Auth correction `35c88a5` is source-integrated locally while preserving the concurrent Health/File/Tag tree. The recovered-tree auth matrix is 189/189 using temporary loopback fakes, and the post-integration full TypeScript/diff gates are green. Worker evidence is 205/205 + 36/36 + 49/49; fresh independent auth acceptance remains mandatory.
- Independent FILE review found three P1 gaps beyond the earlier 113/113 matrix: indeterminate multipart drain, syntactic-only object-version acceptance, and DNS outside the scanner deadline. All three are now corrected locally with direct adversarial regressions; the expanded FILE matrix is 115/115 and full TypeScript/diff are green. Fresh re-acceptance is running; the real AV/CDR provider remains an untouched human gate.
- Independent Health review found no P0/P1 and passed 115/115 functional behavior. Its sole P2 rejection was documentation round-trip loss from two oversized roadmap `Why` fields; those are now within the editor contract, complete detail remains in `Notes`, authored consolidation was regenerated, and the documentation gate is 64/64. Final independent confirmation is running.
- Health Check/Public Funnel identity is now independently accepted locally with P0/P1/P2 none: 115/115 functional, 64/64 documentation/provenance, TypeScript and diff checks all pass. `ABUSE-002`, `HEALTH-ID-001` and `HEALTH-PROMOTE-001` are locally DONE; this does not cover deployment or the separate cross-process production infrastructure gates.
- `FILE-SEC-001` is independently accepted locally with P0/P1/P2 none: 115/115, TypeScript and diff checks pass, and direct exploit probes prove zero-byte chunked rejection, exact provider/key/digest release binding, and an end-to-end DNS/transport scanner deadline. It is locally DONE; `FILE-AV-CONFIG-001` remains an untouched human/provider production gate.
- Auth correction `35c88a5` failed fresh independent acceptance despite 172/172 green tests. Three P1s remain: freelancer provisioning does not bind its exact Supabase subject, late freelancer invitation receipts are not generation-fenced and can leave two live signed tokens, and the login route exports an unsupported helper symbol. `ABUSE-001`, `AUTH-PROVISION-001`, `AUTH-DELIVERY-001` and `AUTH-DELIVERY-GEN-001` are ACTIVE in a third isolated local correction lane; no live action was taken.
- `EMBED-SEC-001` is ACTIVE in a disjoint isolated local correction lane. It is replacing the deployment-wide embed bearer with exact agency/client credentials and a mode ceiling, refusing ambiguous secret ownership, binding immutable tenant/client/origin lineage, atomically consuming nonces once, revalidating active scope, applying durable budgets, and exposing role-gated in-app management. No live configuration, credential, network, database, deployment, push or migration action is authorised.
- Aqua Tag request-boundary review found that admission and form-capture used unbounded `req.json()` and telemetry trusted missing Content-Length before fully draining `req.text()`. A shared byte-counted reader now enforces 160KiB/160KiB/32KiB across declared or streaming bodies, cancels oversize before hydration/proof/storage, rejects non-object JSON, and preserves valid multibyte JSON. Route regression 41/41, direct boundary 8/8, docs 81/81, TypeScript and diff are green; fresh ABUSE-003 acceptance is active.
- Public plugin/commerce/webhook correction is independently accepted at P0/P1 after 246/246 tests and mounted authority/pre-body/shared-secret probes. Its one P2 configuration-wording disclosure is corrected with stable generic no-store webhook errors and preserved retryable status classes; the affected serialized matrix is now 253/253 plus TypeScript/diff. `PUBLIC-WEBHOOK-GENERIC-ERROR-001` remains VERIFY until fresh focused acceptance.
- Fresh Aqua Tag acceptance rejected the body correction on one functional P1 and one test-evidence P2: securely resolved agency master tags returned 404 for every telemetry event, and several adversarial stream cases existed only under `/private/tmp`. Both are corrected locally. Master telemetry now revalidates exact current agency/key/host/source ownership at the sink and records against the source; the permanent suites cover malformed lengths/UTF-8, arrays, truncation, the maximum form contract and zero pre-boundary side effects. Focused **12/12** plus body **22/22** pass; independent re-acceptance is active. Nothing live was contacted.
- Auth third correction `b9f30607` is source-integrated on nine exact-matching files plus a surgical six-field type retirement that preserves concurrent FILE evidence. Exact freelancer provider binding, generation-fenced delivery, bound-subject login and route export legality pass 39/39 in the recovered tree; worker evidence is 224/224 and full TypeScript/diff are green. Fresh independent auth acceptance is active.
- Auth is independently accepted locally with P0/P1/P2 none. The reviewer passed 43/43 focused, 234/234 adjacent, two adversarial concurrency/replay probes, cross-process generation, TypeScript and diff. `ABUSE-001` and its auth sub-gates are DONE in source; real Supabase/email-provider/deployed CAPTCHA verification and applying the additive nonce migration remain release gates and were not touched.
- Aqua Tag is independently accepted locally with P0/P1/P2 none. Exact master/client/agency scope, stale-state refusal, consent lineage, durable retry semantics and byte-bounded pre-hydration request handling passed 74/74 focused, 9/9 telemetry/replay, independent mutation probes, TypeScript and diff. `ABUSE-003`, `TAG-KEYS-001`, `TAG-REPLAY-001` and `TELEMETRY-SEC-001` are DONE in source; migration/provider deployment remains untouched.
- `ERASURE-002` is locally implemented and in VERIFY. One contact-data-free but pseudonymous tombstone is committed atomically with the locked live inbox erasure, and every later identity insert/relink is trigger-refused; failed ownership validation rolls the tombstone back. The independent reviewer found no P0/P1 and one documentation P2; source docs now classify the opaque client id as access-restricted personal data subject to retention review. Final re-acceptance and unapplied-migration staging remain; nothing live was touched.
- `EMBED-SEC-001` is integrated locally and in VERIFY. The global bearer is replaced by encrypted agency/client credentials, immutable mode and credential lineage, active-scope/origin revalidation, durable budget admission, Postgres-or-Supabase single-use nonces, and role/CSRF-gated one-time-secret management. Embed **18/18**, durable nonce **19/19**, semantic/docs **37/37** pass; fresh independent acceptance is active. No network, provider, cloud, database, migration apply, deployment, push, or main action occurred.
- Public webhook final acceptance found no P0/P1 but two P2 contract gaps: Finance and Memberships did not classify equivalent missing configuration consistently, and mutable `Error.name` could enter categorized logs. Both are corrected locally. All five facades now separate 503 missing-config from 400 invalid-signature before body access, and logging uses fixed categories only. Focused **5/5**, adjacent **97/97**, TypeScript/diff pass; a different fresh reviewer is active. Nothing live was touched.
- `ERASURE-002` is independently accepted locally with P0/P1/P2 none and is DONE in source. The exact lock/trigger/tombstone ordering closes delayed relinking without cross-tenant false positives, failures roll back atomically, and the retained opaque id is truthfully classified as pseudonymous personal data under access and retention review. Disposable-PostgreSQL concurrency and applying the migration remain external release gates; nothing live was touched.
- Embed framing P1 is corrected locally without weakening the global clickjacking boundary. Only `/embed/account` receives a dynamic full CSP; its external ancestor comes from verified signed embed-session lineage, while forged/ordinary/unbound sessions are unframeable. The page revalidates live credential lineage before customer data. Focused embed/CSP **27/27**, TypeScript and config checks pass; fresh independent re-review is active.
- Public webhook final acceptance rejected the prior correction with three P1 error-containment gaps and one P2 coverage gap despite 111/111 green tests: Affiliates verifier throws, Commercial post-verification mutation throws, and dispatcher flush logs were not safely contained. A separate local-only correction lane is active; the queue no longer describes this gate as accepted.
- Claude did complete the first loop slice before stopping: isolated commit `02a9b520` implements `SEC-006` subject-access ownership/redaction and reports 43/43 focused/adjacent checks plus TypeScript/diff. It remains VERIFY until the independent Codex reviewer accepts source and behavior; nothing has been integrated or pushed.
- Public webhook containment is independently accepted locally with P0/P1/P2 none after the final unreadable-vault fix. The exact hostile paths now return generic no-store retryable responses and fixed-vocabulary logs; final matrix **115/115**, TypeScript and diff passed. Public plugin tenancy, Commerce, Webhook and generic-error source gates are DONE locally; real Stripe/provider/deployed/durable-DB checks remain release gates.
- Embed security is independently accepted locally with P0/P1/P2 none. Next's real header evaluator, signed-cookie policy matrix, page revocation/lineage matrix, 20/20 focused, 61/61 adjacent, TypeScript and diff all pass. `EMBED-SEC-001` is DONE in source; real deployed headers, cross-browser third-party cookies, provider secrets and nonce/budget migration/concurrency remain release gates.
- Claude `SEC-006` commit `02a9b520` is independently rejected (P0 0/P1 4/P2 3) and will not be integrated. Green tests missed untyped free-text authority, third-party PII leakage/data corruption, client-facet omissions and bypass of the verified SubjectRequest workflow; no-store/bounds/docs were also incomplete. A separate Codex correction lane is active in the integration tree.
- A loopback-only AquaCRM preview is available at `http://127.0.0.1:3002/login`. Fresh Chromium checks at desktop and 390px phone widths returned 200, rendered meaningful content, produced no console/page errors and showed no phone-width horizontal overflow. This is a local playability check only; it does not authorise a push or establish production readiness.
- Claude `PLUGIN-LINEAGE-001` commit `ecf91b21` is independently rejected (P0 0/P1 2/P2 1) and will not be integrated. Hostile Agency Marketing PATCH and mounted Email Sender enqueue paths still forge immutable tenant/person/client/audit lineage; campaign/template PATCH shares the immutable-field weakness. A clean-base disjoint Codex worktree is correcting this now while Claude owns only the full login visual slice.
- `SEC-006` corrective implementation is complete locally and in fresh independent acceptance. Typed authority/review quarantine, verified exact SubjectRequest gating, bounded/no-store delivery, transactional activity+fulfilment, adversarial tests and truthful regenerated docs are green in the implementer lane (65/65 + 46/46 + TypeScript/diff); no DONE claim is made until the independent reviewer accepts it.
- The full feature-gap map is recorded in `FEATURE-GAP-MAP-2026-09-12.md` and QUEUE.md is de-duplicated. Sales flow is implemented but unaccepted and not Sales-staff enabled; Maps embed is provider-dependent while arbitrary Google/web framing is not viable; boards/chrome/check-in/settings are partial; canonical People and a legacy Rolodex coexist; domain routing is missing; Prospect semantics are stale. Explicit child gates now prevent those from being hidden inside broad `CHROME-001` or false-DONE labels.
- `PLUGIN-LINEAGE-001` correction is committed locally at `60f08599` with installed/distributed parity and comprehensive hostile tests; a fresh reviewer owns its verdict. Claude's complete login-only slice (`46f98435` + `b5d2cc5d`) is also in fresh browser/auth/accessibility acceptance. Neither is integrated or marked DONE yet.
- `SEC-006` failed its second fresh acceptance (P0 0/P1 5/P2 2) despite green implementer and adjacent suites. Typed foreign/stale ownership, schema-complete safe projections, nested plugin/sidecar completeness, review-vs-fulfilment state, durable delivery evidence and bounded linear output work remain open. Its consistent in-progress third pass is preserved in explicitly non-release checkpoint `3abd629e`; acceptance is still required before release integration.
- `PLUGIN-LINEAGE-001` also failed fresh acceptance (P0 0/P1 1/P2 1): key allowlisting without value validation allowed corrupt index side effects before a 422 and persisted object-valued notes; malformed/no-op patch containers returned 200. The follow-up now validates the whole mutation before any side effect in both installed and distributed copies.
- Claude committed `ABUSE-BASE-001` infrastructure at `604dfe5e` and moved to the disjoint Settings-scroll item. The limiter has local atomic-adapter/migration tests but is not connected to any admission endpoint, so it remains VERIFY and cannot yet replace the known process-local production controls.
- `LOGIN-UX-001` failed fresh acceptance (P0 0/P1 1/P2 2): 320px CAPTCHA clipping, missing auth-page skip-link targets, and lost reset-password tenant identity. Its otherwise green visual/contrast/focus/security behavior is preserved in a clean-base three-defect correction lane; transient reviewer build artifacts were removed from Claude's worktree without touching source.
- Claude completed the Settings-scroll slice at actual commit `1cc13b2d` and moved to Check-in UX. Settings is source/test green but remains VERIFY pending authenticated browser coverage; its handoff's different SHA is recorded as stale rather than silently trusted.
- Plugin-lineage follow-up `a437a605` is independently accepted with P0/P1/P2 none and 215/215 applicable checks. Reconciliation into the combined continuation branch is active; combined-tree regressions must still pass before release promotion.
- Login correction `52f85e2d` is committed cleanly in its isolated branch with 54/54 browser and 94/94 focused checks, TypeScript and diff green. It remains VERIFY pending independent acceptance; two pre-existing in-process suites hung before registering subtests and are not counted green.
- Claude committed the Check-in decision slice at `7bfe9ffc`; exact timing, scope and non-destructive source behavior are tested, but the reported user-visible disappearance is not closed until an authenticated mounted browser acceptance proves focus/keyboard/unsaved-input behavior. `BRAND-ERASURE-001` is the next authorised Claude queue item.
