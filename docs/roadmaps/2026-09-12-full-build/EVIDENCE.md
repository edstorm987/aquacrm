# Evidence ledger

Append only verified current-run evidence. Include timestamp, queue ID, exact HEAD/diff state, command, exit status, count, and evidence class. Never upgrade focused or local evidence to deployed-live.

## 2026-09-12

- `SEC-006` — isolated-worker handoff — Claude branch `overnight/claude-hardening-20260912`, commit `02a9b520` over `35ceed09` — typed subject-access ownership and third-party redaction in two files. Worker reports new 7/7, existing 5/5, compliance 31/31, TypeScript/diff green. This is handoff evidence, not acceptance; an independent reviewer is reading source and rerunning adversarial cases before any integration. Local only; no remote/live/provider/deploy action.
- `SEC-006` — independent rejection — 2026-09-12, Claude commit `02a9b520` (unchanged on descendant `ecf91b21`) — **REJECT, P0 0 / P1 4 / P2 3**. Direct probes exported Bob-owned records from Alice free-text ID/phone mentions, leaked names/addresses/postcodes/free-text and deep PII, omitted exact client-facet records, and proved the download bypasses the identity-verified tenant SubjectRequest sequence. Failure no-store/bounds/docs tests are also false or absent. Advertised 12/12 and adjacent 43/43 plus TypeScript/diff were false-green. Commit remains isolated; independent corrective lane active. No external action.
- `EMBED-SEC-001` — corrective focused-test — 2026-09-12, recovered dirty integration tree at base `35ceed09d04f707b6ecdbe2ff885fc8161172526` — global CSP stays self-only, while `/embed/account` is excluded from the static CSP and receives a full request-bound policy derived only from a verified signed end-customer embed session. Exact HTTPS origin is allowlisted; forged, ordinary or unbound sessions receive `frame-ancestors 'none'`. Customer data loads only after live credential/version/tenant/client/mode revalidation. Command: `NODE_OPTIONS='--conditions react-server' node --import tsx --test scripts/smoke-aqua-embed.test.ts scripts/smoke-embed-security.test.ts scripts/smoke-stored-code-boundary.test.ts scripts/smoke-local-preview-csp.test.ts`; exit 0; **27/27 passed**. Full TypeScript exit 0; Next config import returned two non-overlapping header rules; scoped diff check clean before authored-doc regeneration. Fresh independent re-acceptance active. Local evidence only; no network/live/cloud/push/deploy/migration action.
- `EMBED-SEC-001` — final independent acceptance — 2026-09-12 10:21 BST, recovered dirty integration tree at base `35ceed09` — **ACCEPT, P0 0 / P1 0 / P2 0**. Actual Next header evaluation proved the static CSP absent for `/embed/account` and descendants but self-only everywhere adjacent; signed-cookie policy probes 5/5 and live page-lineage probes 5/5; focused 20/20, adjacent 61/61, TypeScript/diff green. No second CSP or X-Frame-Options writer found. Real deployed CDN headers, browser third-party-cookie behavior, production keys/vault, and Postgres/Supabase migration/concurrency remain external gates.
- `WEBHOOK-SEC-001` / `PUBLIC-WEBHOOK-GENERIC-ERROR-001` — fresh independent rejection — 2026-09-12, recovered dirty integration tree — P0 0, P1 3, P2 1. Commercial verified-event mutations and Affiliates verifier resolution/invocation can throw outside the generic public boundary; dispatcher persistence-flush logging includes raw `error.message`; permanent adversarial coverage missed these paths. Baseline 111/111, TypeScript and diff passed, so green coverage did not constitute acceptance. Local correction lane active; no external action.
- `WEBHOOK-SEC-001` / `PUBLIC-WEBHOOK-GENERIC-ERROR-001` — final independent acceptance — 2026-09-12, recovered dirty integration tree at base `35ceed09` — **ACCEPT, P0 0 / P1 0 / P2 0**. Unreadable-vault resolution, Affiliates verifier throw, Commercial mutation throw and dispatcher flush all return/log fixed generic categories without reading/reflecting raw error fields; unsigned Commercial requests stop before credential resolution and body access. Final focused/adjacent matrix **115/115**, TypeScript and diff check pass. Public inventory remains 15. Local source acceptance only; real provider signatures/redelivery, production vault, deployed headers/logging, durable DB concurrency and provider endpoint configuration remain release gates.
- `LOCAL-PREVIEW-001` — local browser sanity — 2026-09-12 10:31 BST, recovered dirty integration tree at base `35ceed09`, Next development server bound only to `127.0.0.1:3002` — desktop login, phone-width login and public homepage each returned HTTP 200 with meaningful content and no browser console/page errors. Desktop login had nine interactive controls; phone login had no horizontal overflow (`390px` viewport and document width) and retained both labelled fields plus eight focusable controls. Screenshots: `<TMP>/aquacrm-local-login-20260912.png` and `<TMP>/aquacrm-local-login-mobile-20260912.png`. Local sanity only, not authenticated workflow, accessibility, cross-browser, production-build or deployed acceptance. No live/network/provider/database/push/deploy action.
- `PLUGIN-LINEAGE-001` — independent rejection — 2026-09-12, Claude commit `ecf91b21` — **REJECT, P0 0 / P1 2 / P2 1**. Direct behavioral probes proved Agency Marketing lead PATCH can replace immutable id/agency/client/person/createdAt and caller-supplied history, and the mounted owner/manager Email Sender `internal/enqueue` route can stamp arbitrary client/person/plugin/external lineage even though real event subscribers use container calls. Campaign/template PATCH has the same immutable-field spread weakness. Existing 191/191, TypeScript and diff checks pass but do not cover the hostile paths. Commit remains isolated; Claude correction is queued after the disjoint login UI slice. No external action.
- `SEC-006` — corrective implementation handoff — 2026-09-12, recovered dirty integration tree at base `35ceed09` — exact typed Person/client/facet/relationship ownership and exclusive contact identifiers are release authority; free-text/actor/assignee, shared/conflicting, unclassified, co-mingled and depth-limited rows are withheld into counted review metadata. Export delivery requires a verified open same-agency exact-person access/portability request; response construction, audit activity and fulfilment use one coordinated transaction; request body and every response class are bounded/no-store. Source docs and regenerated authored/reference volumes record review/manual/external-store/retention limitations. Corrective report: runtime/adjacent **65/65**, docs/reference **46/46**, TypeScript/diff green, 9 authored volumes from 164 sources, 2,844 files/9,765 symbols referenced. Fresh independent adversarial acceptance is active; this is not self-acceptance or deployed proof. No external action.
- `PLUGIN-LINEAGE-001` — corrective implementation handoff — 2026-09-12, isolated clean-base commit `60f08599a83d5bc19cff2f34e9a6b215f0609139` — installed built-ins and github-templates now use handler+service mutable-field allowlists and field-by-field immutable reconstruction for lead/campaign/template PATCH; human-mounted Email Sender `internal/enqueue` is removed while typed in-process subscribers/services remain. Permanent hostile tests cover installed/distributed bypasses, exact erasure after attempted rewrite, route absence and dispatcher refusal for every role. Worker evidence: **10/10 focused, 125/125 adjacent, 24/24 generated-module, 23/23 dispatcher**, portal and production-entrypoint TypeScript, diff green. A pre-existing generated SMTP smoke syntax error remains separately identified. Fresh independent acceptance active; no integration/external action.
- `LOGIN-UX-001` — isolated-worker handoff — 2026-09-12, Claude login-only commits `46f98435` + `b5d2cc5d` — one centred card, concise tenant brand lockup, CSS-only teal/aqua/sand field, no marketing/photo/glass/filler; focus-visible and reduced-motion guards, >=44px primary controls, support and `/privacy` link retained while LoginForm is unchanged. Worker reports **7/7**, TypeScript/diff, and 320/390/768/1440 loopback screenshots without horizontal overflow. Fresh independent auth/accessibility/browser acceptance active; ancestor SEC-006/plugin commits remain rejected and are not integration candidates. No external action.
- `SEC-006` — second independent rejection — 2026-09-12, current recovered integration tree — **REJECT, P0 0 / P1 5 / P2 2**. Direct probes released records with valid foreign nested person/client scope or stale explicit owners when the subject contact also appeared; leaked unknown third-party first/last name and NI number; corrupted date/bank strings as phones; quarantined realistic Person history and mandatory finance ledger rows; omitted nested pluginData and an unhydrated sidecar; returned 200 and set `fulfilledAt` despite nonzero co-mingled review; and showed effectively quadratic email scanning (10k benign characters ~466 ms, 100k exceeded 30 s). Transaction rollback, replay, shared-phone refusal, 9/9 focused, 59/59 compliance/retention/ledger/semantic and 111/111 security adjacency were green but insufficient. Third corrective pass active; no external action.
- `PLUGIN-LINEAGE-001` — first corrective acceptance rejection — 2026-09-12, isolated commit `60f08599` — **REJECT, P0 0 / P1 1 / P2 1**. Runtime allowlists copied allowed keys without validating values; a hostile lead PATCH returned 422 only after deleting the true campaign index and creating an `[object Object]` index, and direct object-valued notes persisted in installed and distributed modules. Null/array/prototype-only patch containers became `{}` and returned 200 with no-op/version churn. Immutable lineage, erasure, route removal/in-process enqueue and valid mutable/null-clear paths were green; 10/10 committed, 23/23 dispatcher, 81/81 adjacent, TypeScript/diff passed. Follow-up correction active; no external action.
- `ABUSE-BASE-001` — isolated-worker handoff — 2026-09-12, Claude commit `604dfe5e` — new durable/memory admission adapter, process-local fast prefilter and unapplied fixed-search-path/service-role-only atomic counter RPC migration for IP/subject-digest/tenant-install/provider-budget dimensions. Worker reports **9/9** including 50-way loopback concurrency and failure-shape refusal, plus TypeScript/diff green. The handoff explicitly says no existing endpoint is wired to the abstraction; therefore this remains infrastructure in VERIFY, not proof that process-local launch blockers are closed. No database/network/live/push/deploy/migration action.
- `LOGIN-UX-001` — first independent rejection — 2026-09-12, Claude login-only commits `46f98435` + `b5d2cc5d` — **REJECT, P0 0 / P1 1 / P2 2**. A real local 300x65 challenge inside the 320px layout measured a 262px overflow-hidden host and was clipped by 38px; login/forgot/reset expose the global skip link but no `#main-content`; globally hiding the retired marketing panel removes visible tenant identity from reset-password. Positive evidence: 21 Chromium scenarios, 17 zero-violation axe runs, manual AA contrast 5.76-16.49:1, valid focus/reduced-motion/short-height behavior, mocked MFA/recovery/OAuth, 133/133 auth/privacy, 10/10 bot suite, TypeScript/diff green. Clean-base correction active; reviewer-generated Next artifacts were moved out of the worktree and exact tsconfig churn removed. No external action.
- `SETTINGS-SCROLL-001` — isolated-worker handoff — 2026-09-12, actual Claude HEAD `1cc13b2d` (handoff body incorrectly names `1c67e5b7`) — Settings section rail uses desktop sticky positioning plus bounded internal vertical scroll/overscroll containment, while sub-lg retains the existing select-driven single-column flow and min-width protection. Worker reports **4/4 focused + 28/28 adjacent**, TypeScript/diff green; no authenticated browser acceptance was run. VERIFY only; no external action.
- `PLUGIN-LINEAGE-001` — second corrective handoff — 2026-09-12, isolated follow-up `a437a6055d1bf4e5524efd0b39328b7a46a0bfa0` — installed/distributed validators are parity-identical; reject malformed/non-plain/prototype/accessor/empty/unknown-only containers, validate every mutable value/bound/null semantic, and return typed 422 before index/storage/activity/event effects. Prospective records are validated before re-index/write; tests compare full storage entries, revision and side effects for exhaustive hostile matrices and preserve legitimate fields/null-clears. Worker evidence: **14/14 focused, 129/129 adjacent/erasure, 24/24 templates, 23/23 dispatcher**, portal and standalone production TypeScript, parity/diff green. Fresh independent re-acceptance active; no external action.
- `CHECKIN-UX-001` — isolated-worker handoff — 2026-09-12, Claude commit `7bfe9ffc` — existing internal-operator/active-session SmartWorkSessionMonitor now imports a pure exact decision helper for the 10-minute Aqua-work idle threshold, interaction reset and deterministic five-/ten-minute snoozes. Idle opens a non-modal prompt only; it does not sign out/reload and the sole navigation remains the explicit clock-out review. Worker reports **6/6 focused + 13/13 adjacent**, TypeScript/diff green. Mounted authenticated browser focus/keyboard/unsaved-input behavior remains VERIFY; no external action.

- `SALES-001` — focused-test — recovered dirty tree — combined acquisition access, exact lineage, erasure, device privacy, qualification atomicity, staff scope, Person identity and plugin host gates: **116/116 passed**.
- `SEC-001` — source-review + focused-test — recovered dirty tree at base `d932ce665d9f6afaffa75ed7796a69b17ac7ee58` — anonymous Health Check/public-funnel capture no longer imports or exposes session issuance, returns no capture/user ID, refuses existing identities and completion replay generically, pins the founder funnel, bounds Health Check slots, and commits identity + capture through one PortalState transaction. Command: `node node_modules/tsx/dist/cli.mjs --test --test-reporter=spec src/built-ins/modules/public-funnel/src/__smoke__/funnel.test.ts scripts/smoke-health-check-funnel-journey.test.ts scripts/smoke-public-funnel-port-adapters.test.ts scripts/smoke-hc-leads-pipeline-integration.test.ts`; exit 0; **40/40 passed**. Local evidence only; not deployed and no live mutation performed.
- `SEC-002` — source-review + adversarial focused-test — recovered dirty tree at base `d932ce665d9f6afaffa75ed7796a69b17ac7ee58` — direct public client signup is closed; public magic is sign-in-only; membership creation requires an authenticated tenant-scoped, signed-purpose, 15-minute, atomic single-use invitation; setup updates only an immutable exact Supabase subject after a fresh signed AAL1/AAL2 ceremony; password login resolves exact subject/client/profile metadata and refuses archived or mismatched scope. Independent command: `node node_modules/tsx/dist/cli.mjs --test --test-reporter=spec scripts/smoke-client-portal-invite-admission.test.ts`; exit 0; **13/13 passed**. Fresh `tsc --noEmit` exit 0 and `git diff --check` clean. Earlier combined local auth/portal rerun: **86/86 passed**. Local evidence only; not deployed and no live mutation performed.
- `SEC-004` — source-review + focused-test — 2026-09-12 03:54 BST, recovered dirty tree on `recovery/sales-workflow-recovered-20260912` at base `d932ce665d9f6afaffa75ed7796a69b17ac7ee58` — public website-enquiry email/phone/company and derived Lead evidence is suggestion-only; client attribution requires stored provenance from a configured site route, authenticated manual review, or typed lineage; completion metadata distinguishes routing authority from identity suggestions; Inbox identity writes prove same-agency Lead/Contact existence and refuse conflicting canonical People. Command: `PORTAL_BACKEND=memory NODE_OPTIONS='--conditions react-server' node --import tsx --test scripts/smoke-identity-resolution.test.ts scripts/smoke-inbox-company-routing.test.ts scripts/smoke-aqua-tag-ingestion-order.test.ts scripts/smoke-inbox-element-leaf-access.test.ts`; exit 0; **42/42 passed**. Fresh `tsc --noEmit` exit 0 and `git diff --check` clean. Local evidence only; no live request, deployment, push, or provider/database mutation performed.
- `SEC-005` — source-review + focused-test — 2026-09-12 04:00 BST, recovered dirty tree on `recovery/sales-workflow-recovered-20260912` at base `d932ce665d9f6afaffa75ed7796a69b17ac7ee58` — customer and internal client records accept website enquiries only when exact `clientId` and a recognised server-written `clientLinkSource` agree; email/phone matching was removed from both disclosure predicates. Independent design check confirmed Person reciprocity is a consistency signal rather than an admission requirement because configured routing is authoritative before Person materialisation. Focused portal/identity command exited 0; **60/60 passed**. Local evidence only; not deployed and no live action performed.
- `SEC-007` — source-review + focused-test — 2026-09-12 04:00 BST, recovered dirty tree on `recovery/sales-workflow-recovered-20260912` at base `d932ce665d9f6afaffa75ed7796a69b17ac7ee58` — Journey Lead reads, Contacts pages, Person mutations and linked-client interactions, identity-resolution operations, and enquiry-to-Lead/Contact bridges enforce exact Growth/Inbox/client-Communications element floors before reads or writes; Person timelines accept stored enquiry facet/person lineage only. Focused command covering new leaf contracts plus plugin route classification, interaction, enquiry classification/admission, and public contact tests exited 0; **59/59 passed**. Fresh full `tsc --noEmit` exit 0 and `git diff --check` clean. Local evidence only; not deployed and no live action performed.
- `SEC-003` — independent-review — 2026-09-12 04:00 BST — implementation-focused tests were green, but acceptance remains **open** because the independent reviewer identified missing or vacuous evidence for exact cross-client funnel deletion/preservation, direct second-run idempotency across all hooks, shared Person phone preservation, mutation-then-failure rollback plus successful retry, and secondary-index cleanup. No completion claim recorded.
- `AUTH-001-ECOSYSTEM` — source-review + focused local tests — 2026-09-12 04:26 BST — the shared Aqua bot-challenge script is loaded before interaction and exact `brand-enquiry`/`login` actions are attached across AquaOasis-Web, Zimante, Milesymedia enquiry forms, and Edward Hallam; JSON enquiry callers forward `captchaToken` and reset single-use proof in `finally`; browser login forms rely on page navigation after posting the generated hidden token. Focused source contracts: AquaOasis **3/3**, Zimante **3/3**, Milesymedia **3/3**, Edward Hallam **4/4**; Next TypeScript checks passed for AquaOasis root + nested main-site, Zimante, and Milesymedia; AquaOasis targeted ESLint passed; Edward `node --check app.js`, `vercel.json` parse, and all `git diff --check` gates passed. Local commits: Zimante `845b75912088f87c9c69c3ca28cb5d2b9ba7b354`; Milesymedia `73a30c8f743459c78adfb836662e7f1496113c1e`. AquaOasis remains uncommitted at `60f0caf7f221cc25f075813e97585f599e0f7cd7` because user-owned edits overlap three touched files; Edward Hallam remains uncommitted at `5ee5065ef464c0e2c8e8b4e148054083122d576a` because the sandbox denied `.git/index.lock`. Milesymedia login remains explicitly **open**: it posts to local `/api/client-portal/login` and does not call AquaCRM `/api/auth/login/browser`, so no decorative unverified token was added. No live request, network access, dev server, push, deployment, provider change, or database mutation was performed.
- `AUTH-001` — source-review + adversarial focused local tests — 2026-09-12 04:28 BST — recovered dirty tree on `recovery/sales-workflow-recovered-20260912` at base `d932ce665d9f6afaffa75ed7796a69b17ac7ee58` — managed Turnstile abstraction integrated with exact action/hostname and timestamp binding, bounded replay cache, five-second server timeout, twelve-second client script timeout, fail-closed production configuration, exact external branded-login Origin binding, route-level verification before password/capture work, post-proof victim email/phone quotas, accessible reset/retry states, and a source inventory for all AquaCRM-owned mounted callers. Command: `PORTAL_BACKEND=memory NODE_OPTIONS='--conditions react-server' node --import tsx --test scripts/smoke-bot-challenge.test.ts scripts/smoke-login-bot-challenge.test.ts scripts/smoke-public-forms-bot-challenge.test.ts scripts/smoke-auth-form-encoding.test.ts scripts/smoke-bot-challenge-surfaces.test.ts`; exit 0; **68/68 passed**. Full recovered-tree `tsc --noEmit` exit 0 and `git diff --check` clean. Tests used only an in-process verifier stub and a `127.0.0.1` Supabase stub. No live request, network provider call, deployment, push, secret, or database mutation was performed.
- `PUBLIC-ABUSE-AUDIT` — read-only source/schema/caller review — 2026-09-12 — current recovered dirty tree — confirmed protected login/contact/brand-enquiry ordering, then identified launch-blocking alternate admissions: owner-session-before-email-proof JSON signup; unchallenged password-reset/magic email sends; mounted and duplicate Health Check completions; public-key-authorised Aqua Tag mutations; unchallenged website-editor contact/newsletter; careers files without real AV quarantine; public checkout including zero-value completion; and deployment-wide cross-tenant embed admin authority. Systemic limiter is process-local and not multi-instance evidence. Bounded queue items `ABUSE-001..003`, `FILE-SEC-001`, `COMMERCE-SEC-001`, `EMBED-SEC-001`, `ABUSE-BASE-001`, and `WEBHOOK-SEC-001` were created. No files, network, server, provider, database, or live state were touched by the audit.
- `SEC-003` — independent source/schema review + adversarial focused tests — 2026-09-12 — recovered dirty tree — exact erasure now distinguishes routing from identity authority, preserves shared/legacy/cross-client subjects for review, removes exact acquisition/public-funnel data idempotently, rolls plugin/local mutations back on failure, and deletes inbox data through one typed `erase_client_inbox_data` RPC. Local migration `supabase/migrations/20260912130000_atomic_client_inbox_erasure.sql` uses fixed search path, service-role-only execution, locks the real connection/identity/conversation/message chain, validates same-agency ownership, deletes exact identity roots with cascades, verifies the post-delete invariant, and returns no-PII counts/span. Full 10-file matrix exit 0: **220/220 passed, 34 suites**; full `tsc --noEmit` exit 0; `git diff --check` clean; independent final review found no P0/P1 within the RPC/parser/fake/tests. Migration remains **unapplied** and must precede the server deploy after staging concurrency testing. No network, live database/provider action, commit, push, or deployment occurred in this evidence run.
- `EDITOR-LOCAL-GATE` — complete local smoke suite — 2026-09-12 — clean checkpoint `35ceed09d04f707b6ecdbe2ff885fc8161172526` — command `npm run smoke:website-editor`; exit 0; **49/49 files passed in 13.8s**. This exercised only repository tests with local/in-memory fixtures; no server, network, provider, database, deployment, push, or live state was used. This is a scoped Website Editor gate, not a production-readiness verdict.
- `TYPECHECK-LOCAL-GATE` — clean-checkpoint TypeScript verification — 2026-09-12 — checkpoint `35ceed09d04f707b6ecdbe2ff885fc8161172526` — command `node node_modules/typescript/bin/tsc --noEmit`; exit 0 with no diagnostics. No server, network, provider, database, deployment, push, or live state was used.
- `BUILD-LOCAL-GATE` — isolated production compiler/bundler verification — 2026-09-12 — clean checkpoint `35ceed09d04f707b6ecdbe2ff885fc8161172526` with no `.env` file — command `npm run build`; exit 0. Next.js 16.3.0 Webpack compiled successfully in 72s, TypeScript finished in 42s, and all **247/247** static pages generated; post-build repository status and `git diff --check` remained clean. Network access was unavailable and no server, live/provider/database/deployment/push action occurred. This proves a local production build, not deployed readiness.
- `ABUSE-001..003-INTEGRATION` — local source integration + combined regression — 2026-09-12 — clean base `35ceed09d04f707b6ecdbe2ff885fc8161172526`; worker commits `9b7a8c670e2228b1daf9679c2fe74fd71e75b1e6`, `441f4e7511323c5b9ee9b7e69cbf2a656772005a`, and `1d206fe3fe2091d6bf600ce33fd09fa835c44037` were applied at source level into the recovered integration tree. Serial matrix across every changed/new test file: **246/246 passed, 46 suites**; combined full `tsc --noEmit` exited 0; `git diff --check` clean. Independent combined-tree adversarial review remains in progress before checkpoint/DONE status. No network, live/cloud/provider/database/deployment/push/main action occurred.
- `ABUSE-COMBINED-INDEPENDENT-REVIEW` — read-only combined-tree adversarial review — 2026-09-12 — rejected the initial green claim and kept the gates active. Confirmed P0: Aqua Tag admission was an unauthenticated signing oracle because public site key plus spoofable `Origin` could sign attacker-selected payload; no human proof existed. Confirmed P1s: repeated admissions charged quotas before idempotency; emitted client/agency tag-key classes lacked resolver support; non-hardcoded telemetry keys lacked host binding; anonymous Health Check created a global User and could pre-register a victim email; password reset lacked purpose/epoch/exact-subject/provider-local convergence; magic delivery/session revision was incorrect; security links trusted request/custom origin; agency verification could reissue setup capability; website-lead routing trusted posted brand/cookie tenant hints. Source-string tests were not accepted as behavioral proof. Corrections were assigned back to three isolated branches. No network, live/cloud/provider/database/deployment/push/main action occurred.
- `ABUSE-002-IDENTITY-FOLLOWUP` — source integration + focused local regression — 2026-09-12 — follow-up commit `41e1de515b081fe33c9b867b89e82c5fae683498` applied into the recovered tree. Anonymous Health Check capture uses an opaque plugin-only `pendingLeadId` and creates no global User/password/session/membership/provider identity or authenticated actor; canonical repeats and existing identities fail closed; pending activity/ledger erasure and legacy capture-created User cleanup remain separate. Combined integration command across funnel, Health Check abuse/journey, port adapter and client erasure suites: **100/100 passed, 23 suites**. Worker broader matrix **169/169**, full typecheck and diff check passed; independent combined review still pending. Local only; no network/live/cloud/provider/database/deploy/push.
- `ABUSE-001-AUTH-FOLLOWUP` — source integration + focused local regression — 2026-09-12 — corrective commit `a967871f80fd6514716512a7b6c6fa7119ab3b04` applied into the recovered tree. It adds canonical configured security-link origins; strict setup request origin/content type; exact reset purpose/session epoch and sibling invalidation; durable resumable provider receipt with exact bound client subject and local revocation before success; per-generation magic delivery idempotency/current session revision; setup-link expiry; exact WebsiteSource tenant authority; server-required consent; explicit verification/magic flushes and generic failures. Combined auth/reset/magic/signup/lead/freelancer matrix: **92/92 passed, 17 suites**. Worker evidence: 81/81 focused plus 117/117 broader, full typecheck/diff check. Independent combined review and one loopback invitation harness remain pending. Local only; no network/live/cloud/provider/database/deploy/push.
- `ABUSE-002-INDEPENDENT-REJECTION` — read-only source review + adversarial local probes — 2026-09-12 — corrective follow-up `41e1de51` is **not accepted**. No P0 was found, but three P1 invariants fail: the public Health Check completion route calls `seedFounder()` and can create an authenticatable agency owner/Agency/outbox identity on fresh configured state; canonical pending-capture uniqueness is install-scoped and a two-agency/two-install concurrent probe admitted the same email twice; capture events persist email and pending ID into `automationRuns`, while exact/client erasure and retention do not sweep those derivatives. The public-funnel metadata also contradicts the pending-only implementation. Earlier 100/100 green tests pre-seeded founder, used one install, or omitted automation derivatives, so they are retained only as scoped evidence. Correction was reassigned locally. No network/live/cloud/provider/database/deploy/push/migration apply.
- `ABUSE-003-PUBLIC-CAPTURE-FOLLOWUP` — source integration + worker regression — 2026-09-12 — corrective commit `072987f27c7f1c111ca3797b114d27e1fb63242a` applied source-level into the recovered tree; all 23 files match the isolated commit. The mounted Aqua Tag now obtains exact-action human proof, admission binds immutable key/tenant/site/registered-host/form/action/facts, all emitted key classes resolve, telemetry remains non-CAPTCHA but exact-host validated and sanitized, and additive service-role-only claim/complete/release RPCs provide durable replay/conflict/receipt/lease fencing with missing-migration failure closed. Worker evidence: **116/116** tag/abuse/telemetry/RLS/routes, **144/144** SEC/role/lineage, **6/6** loopback multi-process collision/recovery, TypeScript and diff check green. Independent integrated-tree review is active; additive migration remains unapplied and disposable-Postgres acceptance was not run. Local only; no external network/live/cloud/provider/database/deploy/push/main action.
- `AUTH-INVITATION-LOOPBACK-REGRESSION` — local loopback behavioral test — 2026-09-12 — first sandbox run could not bind `127.0.0.1`; approved local-only rerun completed and found **5/13 passed, 8/13 failed**. The harness does not configure the new canonical `NEXT_PUBLIC_PORTAL_BASE_URL`, so delivery is skipped, issuer returns 503, and redirect error assertions observe no code; independent auth review is determining test staleness versus source defects. This gate remains open. The server was an in-process Supabase stub only; no external/live/cloud/provider/database/deploy/push action occurred.
- `AUTH-INVITATION-LOOPBACK-DIAGNOSIS` — local loopback behavioral test — 2026-09-12 — rerunning the same 13-case harness with canonical local `NEXT_PUBLIC_PORTAL_BASE_URL=http://localhost:3099` produced **13/13 passed**. This proves the earlier 8 failures are a stale harness-environment omission caused by the new fail-closed configured-origin contract; the test must be reconciled to save/set/restore its own local canonical origin. Source acceptance still awaits the independent auth review. In-process `127.0.0.1` stub only; no external/live/cloud/provider/database/deploy/push.
- `ABUSE-002-SECOND-FOLLOWUP` — source integration + worker regression — 2026-09-12 — second corrective commit `96a003de12f83e5388af6b0ef8c50d0c65c97bcc` applied source-level into the recovered tree; all seven files match the isolated commit. The public route no longer seeds founder/privileged state, the global state transaction scans authoritative public-funnel capture rows across installs for canonical pending-email uniqueness, durable automation-run event fields exclude email/pending ID/full slot answers while the in-memory typed subscriber retains required data, and docs describe pending-only fail-closed semantics. Worker evidence: focused 11/11, funnel/erasure/automation/signup **134/134**, auth/session/lead/BOS **164/164**, tsc/diff green. Second independent review is active. Local only; no network/live/cloud/provider/database/deploy/push/migration apply.
- `AUTH-INVITATION-HARNESS-RECONCILED` — local loopback behavioral test — 2026-09-12 — harness now saves, sets, and restores `NEXT_PUBLIC_PORTAL_BASE_URL` to its own loopback origin; the ordinary command passed **13/13**. In-process `127.0.0.1` Supabase stub only; no external/live/cloud/provider/database/deploy/push.
- `ABUSE-001-AUTH-INDEPENDENT-REJECTION` — read-only source review + mounted/adversarial local probes — 2026-09-12 — follow-up `a967871f` is **not accepted**. No P0 was found, but six P1 families remain: documented production `PORTAL_BACKEND=supabase` selects an in-memory nonce adapter, allowing cross-instance replay; mounted password-reset issuance uses unscoped email and cannot reach a scoped end-customer while a same-email owner can be selected; magic tokens omit issuance epoch and a pre-reset link redeemed after password rotation; agency signup resumed `provider-ready` with a changed password and split provider/local credentials; portal invitation eligibility is emailed before flush and customer setup lacks a resumable exact-subject provider operation/session bump; known-account magic/reset delivery exceptions throw while unknown accounts return generic 200. P2s include permissive customer setup Origin/content type, raw provider messages, private/unspecified IPv6 public-origin acceptance, verification nonce/state crash window, incomplete consent evidence, timing differences, and unstable freelancer delivery retry. Review evidence: 77/77 scoped tests, 13/13 reconciled invitation, 4/4 origin tests, diff check clean—but those suites missed the mounted/cross-process invariants. Correction reassigned locally. No external network/live/cloud/provider/database/deploy/push/migration apply.
- `ABUSE-003-INDEPENDENT-REJECTION` — read-only source/SQL review + adversarial local probes — 2026-09-12 — corrective `072987f2` is **not accepted**. No P0 was found, but three P1s remain: routing normalization strips `www.` before exact Turnstile hostname verification, rejecting legitimate provider proofs; telemetry resolves exact host-bound tenant/client scope then discards it and persists by first site-key match, allowing duplicate/corrupt key state to misroute across tenants; resolved PostgREST transport/status-0 errors are marked `rollbackConfirmed`, so a lost completion response can refund quotas after a possible commit. P2: migration adoption copies the differently canonicalized legacy fingerprint and hardcodes `attached:true`, so legacy exact retries conflict and original tag-first receipts are falsified. Positive apex-host/proof exclusion/fresh claim/SQL grant controls remain. Reviewer evidence: 112/112 focused local tests, 6/6 loopback multi-process, tsc/diff green—but tests omitted www, duplicate keys, real resolved error shape and legacy-row upgrade. Correction reassigned; migration unapplied. No external network/live/cloud/provider/database/deploy/push.
- `ABUSE-002-SECOND-INDEPENDENT-REJECTION` — read-only source review + adversarial full-state local probes — 2026-09-12 — second correction `96a003de` is **not accepted**. No P0 was found; one P1 remains. Automation-run sanitization does not alter the raw plugin subscriber event: a valid anonymous Health Check created durable Lead, acquisition profile, email-pointer, Prospect, Person, pipeline-card and `person.created` outbox derivatives containing or referencing the submitted identity. Exact public-funnel erasure returned success and removed the pending capture but left all seven downstream email paths because the subscriber discarded capture linkage. Fresh unconfigured state remained byte-identical at 503; a 50-way/two-install race admitted exactly one canonical address while 20 unrelated addresses remained isolated; delayed automation runs were non-PII; focused 17/17 and storage/funnel 42/42, tsc/diff green. P2: abuse test provisioning is order-dependent and three historical/current-state docs contradict pending-only semantics. Required correction is strict pending-only anonymous capture plus a distinct idempotent verified/authenticated promotion event with exact linkage. Reassigned locally. No external network/live/cloud/provider/database/deploy/push.
- `FILE-SEC-001-LOCAL-IMPLEMENTATION` — source implementation + focused local behavioral tests — 2026-09-12 — public careers submission obtains exact-action managed human proof before victim-address quota and file/storage work, cannot bootstrap founder state, and passes complete file bytes to a credentialed HTTPS AV/CDR gateway with redirect refusal, bounded response and digest/type metadata. Signature trust remains separate from malware verdict: absent/unavailable/legacy scanner evidence is stored privately as quarantined and every operator read fails closed; only explicit scanner-clean release downloads, as attachment with `nosniff` and sandbox CSP. An authorised People manager can retry a quarantined scan after outage recovery; production env validation requires scanner URL/token. Combined content-trust/public-form/private-upload matrix **55/55**; env/readiness **42/42**; full `tsc --noEmit` exit 0; `git diff --check` clean. Independent review pending. No scanner/provider/network/live/cloud/database/deploy/push/main action occurred.
- `ABUSE-003-THIRD-FOLLOWUP` — source integration + worker local regression — 2026-09-12 — follow-up commit `aff2aaecaad26758d0c583bc16bb4901e740b51f` (parent `072987f27c7f1c111ca3797b114d27e1fb63242a`) applied source-level into the recovered integration tree. All 13 source/test/migration files match the isolated worker exactly; the two documentation deltas were reconciled with concurrent FILE-SEC documentation. The correction preserves exact challenge hostname separately from canonical routing host, revalidates exact resolved tenant/client/site telemetry scope, treats status-0 completion as ambiguous with no quota refund, and adopts only provable legacy tag-first rows with `attached:false` while quarantining ambiguous rows. Worker evidence: focused **121/121**, SEC/role/lineage **144/144**, local `127.0.0.1` multi-process **6/6**, TypeScript and diff check green. Independent re-review active. Disposable PostgreSQL remained skipped, migration unapplied, and no external network/live/cloud/provider/database/deploy/push/main action occurred.
- `ABUSE-002-THIRD-FOLLOWUP` — source integration + worker local regression — 2026-09-12 — follow-up commit `b71404181b28cfdd271d21070b8771e55b44fdc9` (parent `96a003de12f83e5388af6b0ef8c50d0c65c97bcc`) applied source-level into the recovered integration tree; all 24 files match the isolated worker exactly. Anonymous Health Check completion now persists only one pending capture and a sanitized pending event, with no auth, Lead, acquisition, email-index, Prospect, Person, pipeline-card or identity-outbox derivative. Promotion is a distinct server-only idempotent command requiring exact mailbox proof or authenticated authority and records capture lineage. Worker evidence: focused **122/122**, broad funnel/leads/erasure/signup/automation **208/208**, auth/session **128/128**, local loopback **19/19**, TypeScript and diff check green. Supplemental docs were 32/33 only because the isolated worktree lacks `.artefacts/ui-wave9/03-write-journeys.md`; that pre-existing fixture absence is not promoted to green. Independent re-review active. No external network/live/cloud/provider/database/deploy/push/main action occurred.
- `ABUSE-003-THIRD-INDEPENDENT-REJECTION` — read-only source review + local adversarial probe — 2026-09-12 — `aff2aaec` is **not accepted**. No P0 was found and the prior exact-host, resolved client telemetry, status-0 no-refund and legacy-adoption corrections were verified, but one P1 remains: `resolveAgencyByMasterSiteKey` selects the first owner of a duplicated agency master key, so Aqua Tag admission sees one candidate and can mint into the wrong tenant. A local two-agency/shared-key/shared-host probe returned the first agency instead of `null`; existing tests covered client-key but not master-key collision. P2: durable consent-event rows retain site key/origin but omit immutable resolved agency/client/site lineage. Reviewer focused suites **28/28** plus supporting **37/37** and diff check were green, demonstrating the missing invariant rather than acceptance. Correction reassigned. No network/live/cloud/provider/database/deploy/push/migration apply.
- `ABUSE-002-THIRD-INDEPENDENT-REJECTION` — read-only source review + local adversarial probes — 2026-09-12 — `b7140418` is **not accepted**. No P0 was found and anonymous capture itself is verified pending-only/PII-free, but two P1s remain. `promotePendingCapture` trusts caller-built `verifiedEmail`/`verificationId` or any nonempty `actorUserId`; a probe promoted with nonexistent actor `not-a-real-user`. Exact capture erasure removes the pending row but leaves a durable automation run carrying its `captureId`, while the test synthetically adds `clientId` and omits that derivative; retention has no Public Funnel capture category. P2: generated built-ins reference still lists removed event/ports/exports. Focused **115/115** and diff check passed, proving the tested slice but not authority or recovery acceptance. Correction reassigned. No network/live/cloud/provider/database/deploy/push/migration apply.
- `ABUSE-001-AUTH-SECOND-FOLLOWUP` — source integration + local regression — 2026-09-12 — correction `cd637e462fccedfbae70750a948309ac5723e64a` (parent `a967871f`) was applied source-level into the recovered integration tree. The additive nonce migration was deliberately renamed from `20260912140000` to `20260912150000` to avoid colliding with the existing Aqua Tag migration version; its test points at the new filename and it remains unapplied. Durable Supabase nonce replay, exact reset audience, token epochs, keyed signup retry, flushed/resumable client setup, generic delivery failures, exact Origin/JSON, consent evidence and public-origin hardening are present. Local loopback invitation/nonce/MFA checks **47/47**; Tag/schema contract reconciliation **27/27**. Worker evidence includes focused 28/28, invitation/MFA 144/144, tenant/storage 153/153, semantic/route 104/104 and TypeScript/diff green. Fresh independent acceptance is active. No live/cloud/provider/database/deploy/push/main action occurred.
- `ABUSE-003-FOURTH-FOLLOWUP` — source integration + worker regression — 2026-09-12 — correction `7edf1238e2a7cd11a741fc2ef17ced98e21dce60` (parent `aff2aaec`) was applied source-level into the recovered tree; all nine source/test files match except the shared API reference, which additionally retains concurrent FILE-SEC and Commerce truth. Duplicate agency master keys and exact-host collisions now fail closed; durable consent rows retain immutable resolved agency/client/site/key/host lineage without visitor/proof data. Worker evidence: focused **111/111**, documented readiness/security/data group **152/152**, broader lineage/role **132 pass + 1 intentional skip**, loopback **6/6**, TypeScript/diff green. The Dev API endpoint list and combined schema inventory were additionally reconciled locally and pass **27/27**. Fresh independent acceptance remains required; additive migration unapplied. No live/cloud/provider/database/deploy/push/main action occurred.
- `COMMERCE-SEC-001-LOCAL-IMPLEMENTATION` — source implementation + adjacent local regression — 2026-09-12 — public checkout now binds the caller-selected plugin scope to one globally unique registered storefront `Origin`, verifies exact paid or free-order managed-proof action before mutation, compares proof class to a read-only authoritative quote or idempotent operation snapshot, and atomically budgets one-way-digested IP/email/provider/install/tenant/client/country/inventory dimensions. The Website Editor exposes anonymous commerce scope only for an actually published tree; draft/preview/admin renders retain authenticated paths. Wrong-class replays cannot release an existing provider operation, and public return URLs use the already-verified storefront Origin. Checkout/order/rate-limit/Website Editor regression **48/48**; combined `tsc --noEmit` and `git diff --check` green. Independent acceptance and real Stripe/Turnstile staging remain; no provider/network/live/cloud/database/deploy/push/main action occurred.
- `ABUSE-001-AUTH-SECOND-INDEPENDENT-REJECTION` — read-only source review + mounted/adversarial local probes — 2026-09-12 — `cd637e46` is **not accepted**. No P0 was found. Three P1s remain: resumable provisioning writes and trusts recovery authority in subject-editable Supabase `user_metadata`; public agency signup has byte-equivalent but timing-distinct existing/fresh paths; and timeout/ambiguous magic/reset retries can mint a new bearer/provider operation while the aborted prior task may still deliver. P2s: wildcard/reserved/non-routable/multicast public origins accepted, raw signup provider reasons persisted, and freelancer delivery exceptions can escape the structured contract. Systemic process-local subject budgets remain `ABUSE-BASE-001`. Verified positives include durable Supabase nonce selection/two-process replay/missing-RPC fail-closed, exact reset audience, epoch-before-nonce checks, flushed/resumable exact-subject client setup, password fingerprinting and consent evidence. Reviewer suite **107/107** passed, demonstrating test incompleteness rather than acceptance. Correction reassigned locally; migration unapplied and no live/cloud/provider/database/deploy/push/main action occurred.
- `ABUSE-002-FOURTH-FOLLOWUP` — source integration + focused local regression — 2026-09-12 — correction `c197af250fe964857e19451a85ff6f8325fcad64` (parent `b7140418`) was applied source-level into the recovered tree; all 17 changed files match the isolated worker. Promotion now accepts only an opaque credential and an injected host verifier; the host resolves a signed fresh non-demo agency session, real user/email/role/membership/install scope, and refuses forged/cross-tenant/client/stale credentials. Mailbox receipt authority deliberately fails closed until a durable receipt exists. Exact pending erasure removes capture/activity/ledger/automation lineage; promoted rows route into linked CRM erasure; retention sweeps natural Public Funnel automation receipts. Integrated Health/funnel/erasure/retention matrix **157/157**. Worker evidence: broad 213/213, focused 98/98, auth/session 73/73, loopback 19/19, docs 30/30, TypeScript/diff green. Fresh independent acceptance is active. No live/cloud/provider/database/deploy/push/main action occurred.
- `FILE-SEC-001-INDEPENDENT-REJECTION` — read-only source review + local adversarial probes — 2026-09-12 08:01 BST — the first FILE implementation is **not accepted**. No P0 was found. Four P1 invariants remain: the careers route drains multipart bytes through `formData()` before managed proof, rescan can expose clean in-memory state before durable persistence succeeds, download does not re-hash the exact bytes it serves against trusted scan metadata, and scanner URL checks lack DNS resolution/pinning or an exact configured origin allowlist. P2 follow-ups require signed double-submit rescan CSRF, provider-cost/concurrency admission and durable actor/scan/object/digest audit lineage. Verified positives remain scoped: full-blob scanning, fail-closed absent/outage/legacy state, redirect/non-HTTPS/literal-loopback/timeout/oversize refusal, tenant/role/element checks, quarantine, attachment-only download, `nosniff`, `no-store` and sandboxing. Correction is active directly in the recovered integration tree. No network, live scanner, cloud/provider/database/deploy/push/main action occurred.
- `PUBLIC-PLUGIN-TENANT-001-INVENTORY` — read-only source classification — 2026-09-12 08:01 BST — enumerated all **15** mounted `public: true` plugin routes across affiliates, agency-finance, memberships, ecommerce, leads-pipeline, email-sender and website-editor. The catch-all dispatcher intentionally uses query/header tenant scope for anonymous routes, so each handler must independently establish exact install authority. Local source shows strong site/Origin/proof admission on Website Editor contact/newsletter and the corrected ecommerce checkout, while provider webhook install ownership, Postmark query-secret transport, storefront catalogue/quote/receipt host binding and intentional public-blog semantics remain open or awaiting adjudication. A fail-closed typed authority registry and exact shared-secret/cross-install adversarial matrix are specified in `PUBLIC-PLUGIN-TENANT-INVENTORY.md`. No code, network, provider, live database, deployment, push or main action occurred.
- `ABUSE-002-FOURTH-INDEPENDENT-REJECTION` — read-only source review + adversarial local probes — 2026-09-12 — `c197af25` is **not accepted**. No P0 was found. Three P1s remain: promotion verifies a session credential before waiting for the durable lock and does not revalidate immediately before mutation, so authority revoked while blocked still succeeds; the same credential and operation id can promote two different captures because the command is not capture-bound or globally atomically claimed; and promoted-capture erasure only throws `capture_erasure_requires_promoted_lineage` with no production bridge into exact linked CRM erasure, leaving the capture and repeated subject data behind. P2: changed generated docs retain stale consolidation hashes/provenance. Positive local evidence: Health/plugin abuse **36/36**, client erasure **54/54**, journey/adapters **17/17**; pending-only capture, forged/wrong-scope/stale refusal, natural automation retention and exact pending cleanup remain verified. A fifth local correction lane is active. No live/cloud/network/provider/database/deploy/push/main action occurred.
- `PUBLIC-PLUGIN-AUTHORITY-REGISTRY-LOCAL` — source implementation + local behavioral regression — 2026-09-12 — every mounted anonymous plugin route now declares one of five typed authority classes and the generic dispatcher returns fail-closed `public_route_unclassified` for a missing or invented class. The complete shipped inventory remains exactly **15** routes. All ecommerce storefront catalogue, product, quote and receipt facades now bind the caller-selected install back to one globally unique registered WebsiteSource using exact `Origin`, or for browser GETs the exact origin portion of `Referer`; conflicting/malformed headers fail closed before limiter/storage work. Public plugin tenancy + registry suites **32/32** and ecommerce/Website Editor adjacent matrix **52/52** passed; `git diff --check` is clean. Provider-event ownership and independent acceptance remain open. No network/live/cloud/provider/database/deploy/push/main action occurred.
- `PUBLIC-PLUGIN-PROVIDER-BOUNDARY` — local source correction + focused regression — 2026-09-12 — recovered integration tree at base `35ceed09d04f707b6ecdbe2ff885fc8161172526` — all six public provider-webhook handlers now use one bounded raw-body reader, reject missing signature/capability before body consumption, preserve exact signed bytes, reject declared and chunked bodies above 256 KiB, and emit generic no-store failures. Finance, Leads, Ecommerce, and the pre-existing Memberships path require exact signed tenant scope before mutation; Finance also refuses invoice/client disagreement. Postmark secret comparison is constant-time. Provider/module matrix exited 0: **76/76 passed**. Close-deal finance regression rerun exited 0: **23/23 passed**. Local evidence only; Affiliates cross-install ownership and Postmark query-secret transport remain open, fresh independent acceptance is required, and no network/live/provider/database/deployment/push/migration action occurred.
- `COMMERCE-PUBLIC-BOUNDARY` — local source correction + focused regression — 2026-09-12 — recovered integration tree at base `35ceed09d04f707b6ecdbe2ff885fc8161172526` — every public storefront facade requires one exact registered host before storage work; POST uses exact Origin, same-origin browser GET may use exact Referer, conflicting/malformed authority fails closed, and production nonstandard HTTPS ports are rejected. Quote and checkout JSON is streamed and capped at 64 KiB for declared or chunked bodies. Commerce/Website Editor adjacency matrix exited 0: **52/52 passed**; public checkout/order/authority subset exited 0: **28/28 passed**. Local evidence only; independent runtime/CORS/provider acceptance and staging remain open, with no external action performed.
- `PUBLIC-PLUGIN-PROVIDER-OWNERSHIP` — local source correction + adversarial focused tests — 2026-09-12 — recovered integration tree at base `35ceed09d04f707b6ecdbe2ff885fc8161172526` — Affiliates Connect now owns a separate vault-backed endpoint secret and deliberately discards the Ecommerce endpoint secret while retaining the exact client-scoped Stripe API key. Postmark refuses query-carried credentials, accepts bounded Basic/header capability without ambiguity, compares in constant time, resolves exact local MessageID ownership before any write, and commits idempotency plus side effects under the host durable transaction. Affiliates/settings/registry/tenancy matrix exited 0: **123/123 passed**. Postmark/webhook boundary matrix exited 0: **28/28 passed**, including shared-secret cross-install and concurrent duplicate probes. Local evidence only; fresh independent acceptance and real provider staging remain open, and no network/live/provider/database/deployment/push/migration action occurred.
- `FILE-SEC-001-FOLLOWUP` — local correction + worker behavioral verification — 2026-09-12 — recovered integration tree at base `35ceed09d04f707b6ecdbe2ff885fc8161172526` — careers admission now proves the bot challenge and enforces metadata/byte boundaries before multipart drain; scanner egress uses exact configured origin, DNS classification and pinned connection IP with TLS SNI, redirect refusal and bounded responses; durable quarantine/release is exact-object-version transactional; served bytes are re-hashed; CSRF, tenant/element/role, concurrency and durable actor/tenant budget gates are in place; audit references are PII-free and signature trust remains distinct from malware-clean. Worker matrix exited 0: **113/113 passed**; `git diff --check` clean. Full tsc had no FILE errors and only saw two then-in-flight Health edits, which the Health worker subsequently reports corrected. Fresh independent FILE acceptance is active. No network/live/scanner/provider/database/deploy/push/migration action occurred.
- `ABUSE-002-HEALTH-FIFTH-FOLLOWUP` — local correction + worker behavioral verification — 2026-09-12 — recovered integration tree at base `35ceed09d04f707b6ecdbe2ff885fc8161172526` — promotion authority is revalidated inside the durable held lane; hashed claims bind exact subject/operation/agency/install/capture without storing bearer tokens; promoted-capture and full-client erasure route through an atomic exact CRM graph bridge with rollback/retry, preserving shared/pre-existing/cross-client identities and converging across install-hook order. Public Funnel/Health/adapters **53/53**, full client-erasure **56/56**, provenance **6/6**, total **115/115 passed**; full TypeScript and `git diff --check` passed. Fresh independent Health acceptance remains required. Local only; no network/live/provider/database/deploy/push/migration action occurred.
- `ABUSE-001-SECOND-CORRECTION-INTEGRATION` — local source integration + recovered-tree regression — 2026-09-12 — isolated commit `35c88a5a5c3973150711a4e0e69d605dae06b626` (parent `cd637e462fccedfbae70750a948309ac5723e64a`) was applied source-level into the recovered dirty tree while preserving concurrent Health/File/Tag changes. App-metadata-only recovery provenance, timing-equivalent signup handling, durable exact-subject magic/reset generations, ambiguous-delivery retry fencing, AbortSignal propagation, strict public-origin IP filtering, stable provider errors, and freelancer normalization are present. A first sandbox run was invalid because it could not bind a temporary `127.0.0.1` fake Supabase server. The approved loopback-only rerun exited 0: **189/189 passed** across 13 auth/schema/metadata suites. Fresh full `tsc --noEmit` and `git diff --check` exited 0. Worker evidence remains 205/205 focused, 36/36 adversarial fake-Supabase/nonce, and 49/49 Website Editor. Fresh independent auth acceptance is still required; no external network/live/cloud/provider/database/deploy/push/migration action occurred.
- `RECOVERED-TREE-TYPECHECK-AFTER-FILE-HEALTH-AUTH-PUBLIC` — local compile — 2026-09-12 — after direct FILE/Health corrections, root public-provider/storefront changes, and source integration of auth `35c88a5`, `node node_modules/typescript/bin/tsc --noEmit` exited 0 with no diagnostics under the shared heavy-job lock; `git diff --check` also exited 0. This is compile evidence only, not independent security or deployed acceptance. No external action occurred.
- `FILE-SEC-001-SECOND-INDEPENDENT-REJECTION` — read-only source review + local behavioral probes — 2026-09-12 08:46 BST — the 113/113 follow-up was **not accepted**. Three P1s remained: a valid-proof chunked/no-Content-Length multipart body could be fully drained before its aggregate size was known; a scanner-clean release with a forged but syntactically valid object version served when bytes matched the stored digest; and scanner DNS resolution was awaited outside the effective 20-second deadline so a late injected transport result could be accepted. No P0 or additional P2 was found. Existing CSRF, tenant/element/role, atomic release, persistence-failure, pinning/SNI, redirect, response-bound, budget/concurrency and audit behavior remained independently verified. No network/live/scanner/provider/database/deploy/push/migration action occurred.
- `FILE-SEC-001-THIRD-CORRECTION` — local fail-closed correction + behavioral regression — 2026-09-12 — careers now requires a finite valid Content-Length and cancels indeterminate/chunked multipart before challenge or parsing; operator download derives the canonical SHA-256 version from provider + key + digest and refuses a released record that is not exactly bound; one abort race bounds DNS and transport, checks abort before and after each phase, and cannot be extended above 20 seconds. The expanded exact FILE matrix passed **115/115**, including direct chunked-cancel, forged-version and delayed-DNS/no-transport tests; full `tsc --noEmit` and `git diff --check` exited 0. Independent adversarial re-acceptance is active. No network/live/scanner/provider/database/deploy/push/migration action occurred.
- `ABUSE-002-DOCS-ROUNDTRIP-CORRECTION` — local documentation correction + regeneration — 2026-09-12 — independent Health review found no P0/P1 and passed its 115/115 functional gate, but rejected on P2 because two ABUSE-002 roadmap `Why` values exceeded the Dev editor's 400-character contract. Both were split into concise `Why` plus complete `Notes`, the nine authored volumes and provenance manifest were regenerated from 164 sources, and the exact authored/reference/Dev Docs/roadmap gate passed **64/64**, including a byte-identical real-roadmap no-op write. Final independent confirmation is active. No network/live/cloud/database/deploy/push/migration action occurred.
- `ABUSE-002-HEALTH-FINAL-INDEPENDENT-ACCEPTANCE` — fresh read-only source review + behavioral/provenance rerun — 2026-09-12 — **ACCEPT, P0 none, P1 none, P2 none**. Exact Health/Public Funnel/erasure matrix **115/115** passed across 23 suites; authored/reference/Dev Docs/roadmap matrix **64/64** passed across eight suites, including byte-identical real-roadmap no-op persistence. Full TypeScript and scoped diff checks passed. The canonical authored volume embeds the complete corrected roadmap entries and its manifest SHA-256 exactly matches the source. `ABUSE-002`, `HEALTH-ID-001` and `HEALTH-PROMOTE-001` are locally DONE, not deployed. No network/live/cloud/provider/database/migration/deploy/commit/push action occurred.
- `FILE-SEC-001-FINAL-INDEPENDENT-ACCEPTANCE` — fresh read-only review + adversarial local probes — 2026-09-12 08:55 BST — **ACCEPT, P0 none, P1 none, P2 none**. An actual approximately 9 MiB multipart stream with valid proof, chunked transfer and no Content-Length returned 413 with zero bytes produced, cancellation true and no proof/parser work. Matching file bytes plus a forged 64-hex object version returned 423 `cv_quarantined` and served zero bytes. An 80 ms DNS resolver under a 5 ms deadline rejected in 6 ms and invoked transport zero times, including after DNS eventually settled. Exact FILE matrix **115/115** passed across 11 suites; TypeScript and diff checks passed. `FILE-SEC-001` is locally DONE. Real AV/CDR provisioning and production-path observation remain `FILE-AV-CONFIG-001`, human-blocked and untouched. No source edits/network/live/cloud/provider/database/migration/deploy/commit/push action occurred.
- `ABUSE-001-SECOND-INDEPENDENT-REJECTION` — fresh read-only review + hermetic local probes — 2026-09-12 — correction `35c88a5` is **not accepted**: no P0/P2, three P1s. Freelancer provisioning stores the returned Supabase provider id but does not call immutable identity binding, so real setup/reset/login cannot consistently recover the same subject. Freelancer invitation persistence blindly overwrites nonce/session epoch/provider ref, allowing a late receipt from generation N to replace N+1; a probe produced two distinct signed-valid unconsumed reset tokens at the current epoch. The login Next route also exports a non-route helper, which is not a supported App Router route export. Existing auth matrices remained green **172/172**, TypeScript/diff passed, and durable nonce loopback evidence remained valid, demonstrating those gates did not cover the three defects. A third isolated correction lane is active. No edits/network/live/cloud/provider/database/migration/deploy/commit/push occurred in the review.
- `EMBED-SEC-001-ISOLATED-CORRECTION-START` — bounded local work assignment — 2026-09-12 — a disjoint isolated worktree is implementing tenant/client-scoped embed credentials with an immutable mode ceiling, exact token lineage, duplicate-secret ambiguity refusal, active-scope and Origin revalidation, atomic single-use nonce consumption, durable multidimensional budgets, a role-gated in-app management surface, and adversarial tests/documentation. The shared recovered integration tree is untouched by this lane until inspection and acceptance. No network/live/cloud/provider/database/migration/deploy/push/main action is permitted.
- `ABUSE-003-STREAMED-BODY-CORRECTION` — source review + hermetic behavioral correction — 2026-09-12 — Aqua Tag admission and form-capture previously called unbounded `req.json()`, while telemetry trusted absent Content-Length and then fully drained `req.text()`. One generic byte-counted stream reader now caps admission/capture at 160KiB and telemetry at 32KiB, refuses invalid/deceptive declared lengths, cancels missing-length/chunked oversize before hydration/proof/quota/storage, rejects invalid UTF-8 and non-object JSON, and preserves valid multibyte JSON. Relevant route matrix **41/41**, direct boundary regression **8/8**, docs/provenance **81/81**, TypeScript and diff passed. Canonical Aqua Tag documentation and all nine authored volumes were regenerated. Fresh independent ABUSE-003 acceptance is active; additive migration remains unapplied. No network/live/cloud/provider/database/migration/deploy/push/main action occurred.
- `PUBLIC-PLUGIN-INDEPENDENT-P0-P1-ACCEPTANCE` — fresh read-only source review + mounted local probes — 2026-09-12 — **ACCEPT P0/P1; one P2 follow-up**. All 15 anonymous plugin routes exactly match the typed authority inventory; missing/invented authority refused 503 before handler; all six provider endpoints refused missing credentials before body; storefront host/proof/total/durable-budget binding, exact signed provider scope, separate Affiliates endpoint secret and atomic Postmark MessageID ownership held. Focused **169/169** plus mounted **77/77**, total **246/246**, TypeScript/diff passed. P2 `PUBLIC-WEBHOOK-GENERIC-ERROR-001`: arbitrary nonempty Stripe signature headers exposed internal configuration wording on four endpoints, without raw secret bytes or permissive CORS. No edits/network/live/cloud/provider/database/migration/deploy/push occurred in the review.
- `PUBLIC-WEBHOOK-GENERIC-ERROR-CORRECTION` — local source correction + regression — 2026-09-12 — Finance, Ecommerce, Commercial/Leads, Affiliates and Memberships anonymous webhooks now return only stable `webhook_refused`, `webhook_unavailable` or `webhook_processing_failed` envelopes, retain 400 refusal and retryable 500/502/503 provider semantics, and set `cache-control: no-store`; categorized server logs contain no raw body, signature, tenant id or secret value. Leads malformed signed JSON is caught instead of escaping. Direct disclosure/body checks passed **8/8**; the serialized affected authority/tenancy/provider/module matrix passed **253/253**; full TypeScript and diff passed. Fresh independent P2 re-acceptance remains. No network/live/cloud/provider/database/migration/deploy/push/main action occurred.
- `ABUSE-001-THIRD-CORRECTION-INTEGRATION` — local source-level integration + recovered-tree regression — 2026-09-12 — isolated correction `b9f306070d1472f0746598c685dc96eb1da10ce8` (parent `35c88a5a5c3973150711a4e0e69d605dae06b626`) was integrated without overwriting concurrent Aqua Tag/webhook/FILE changes. Nine dedicated files match the clean isolated commit exactly; the six retired invitation fields were removed surgically while preserving 28 concurrent FILE type lines. Exact returned Supabase subject binding precedes freelancer ready/link/complete and is revalidated on replay; freelancer invitations use the durable public-auth generation coordinator; same-email substitution is refused for bound workforce subjects; the login route exports only `POST`. Recovered-tree affected tests passed **39/39**; worker matrix **224/224**; full TypeScript and diff passed. Fresh independent acceptance is active. No network/live/cloud/provider/database/migration/deploy/push/main action occurred.
- `ABUSE-003-MASTER-TELEMETRY-AND-BODY-FOLLOWUP` — local source correction + hermetic regression — 2026-09-12 09:32 BST — fresh acceptance rejected one P1 because `agency-master` scopes resolved securely but the telemetry route dispatched no sink, and one P2 because adversarial body edges lived only in temporary probes. The route now dispatches master telemetry to the agency website sink; the sink revalidates the exact current master key, registered host, source id and agency inside the write, refuses duplicate exact owners, and defaults property attribution to the immutable source id. A hostile Origin and a later exact key/host collision both produce no write. Checked-in regression now covers invalid/oversize Content-Length before stream access, invalid UTF-8, null/arrays, valid-prefix truncation, exact 160KiB 60x2,000-field admission, primitive byte ceilings, stream cancellation and explicit zero calls to hydration/proof/rate/storage. Focused Aqua Tag **12/12** and permanent body suites **22/22** passed. Fresh independent re-acceptance is active; the durable limiter and live PostgreSQL/provider/deployment gates remain separate. No network/live/cloud/provider/database/migration/deploy/push/main action occurred.
- `ABUSE-001-THIRD-INDEPENDENT-ACCEPTANCE` — fresh read-only source review + hermetic and loopback regression — 2026-09-12 — **ACCEPT, P0 none, P1 none, P2 none**. Exact provider subject binding precedes ready/link/complete and completed replay revalidates it; freelancer delivery converges through one durable exact-subject generation and rejects late receipts; workforce login refuses same-email subject substitution; the Next login route exports only `POST`. Focused correction **43/43**, expanded auth **234/234**, independent concurrency and corrupt-replay probes **2/2**, cross-process generation, full TypeScript and diff passed. Nine dedicated files match `b9f30607` exactly and the shared type file retains the correction alongside concurrent security types. `ABUSE-001` and its local auth sub-gates are DONE. Real Supabase metadata/profile behavior, provider idempotency/delayed receipts, deployed CAPTCHA/origin behavior and migration application remain release/human verification. No source edit/network/live/cloud/provider/database/migration/deploy/commit/push/main action occurred in the acceptance.
- `ABUSE-003-FINAL-INDEPENDENT-ACCEPTANCE` — fresh read-only source review + hermetic adversarial regression — 2026-09-12 — **ACCEPT, P0 none, P1 none, P2 none**. Agency-master telemetry dispatches with exact resolved agency/key/source/host scope and revalidates current ownership inside the write; missing tag property defaults to registered source id; duplicate exact owners and hostile origins write nowhere. Client/agency sinks retain their own exact current-scope checks. Permanent body tests cover malformed/deceptive lengths, invalid UTF-8 and object shapes, stream truncation, exact maximum input and zero downstream pre-boundary calls. Focused security/body/routing **74/74**, telemetry/replay **9/9**, an independent stale-scope probe for master-key rotation plus client/agency host changes, full TypeScript and diff passed. `ABUSE-003`, `TAG-KEYS-001`, `TAG-REPLAY-001` and `TELEMETRY-SEC-001` are locally DONE. `ABUSE-BASE-001`, the unapplied migration and live PostgreSQL/provider verification remain separate. No edits/network/live/cloud/provider/database/migration/deploy/push/main action occurred in the review.
- `ERASURE-002-LOCAL-CORRECTION` — local source/schema/docs correction + hermetic regression — 2026-09-12 — the additive inbox-erasure migration now writes one PII-free `(agency_id, client_id)` tombstone inside the same locked transaction and a `BEFORE INSERT OR UPDATE` trigger prevents delayed or future identity relinking. The fake Supabase model proves writer-first deletion, erasure-first denial semantics, durable zero-row denial, and rollback of both deletion and tombstone when ownership validation fails. Erasure/schema tests **60/60** and semantic/authored/reference/Dev Docs tests **37/37** passed; nine authored volumes were regenerated from **164** sources. Fresh independent review and applying/staging the migration remain open. No external network/live/cloud/provider/database/migration apply/deploy/push/main action occurred.
- `EMBED-SEC-001-INTEGRATED-CORRECTION` — local source integration + hermetic regression — 2026-09-12 — isolated commit `c37709759314ddf07e4044a2a9274e105c3ba88f` was integrated source-level while preserving concurrent auth, Aqua Tag, FILE, and public-webhook work. The deployment-wide bearer is removed in favour of encrypted agency/client credentials with an immutable mode ceiling, duplicate-secret refusal, one-time disclosure, exact token credential lineage, live revocation/version checks, optional exact HTTPS origin binding, atomic durable budgets and single-use nonce exchange. Current production consume accepts both Postgres and Supabase durable nonce adapters and rejects memory; `aqua-embed` is admitted by the typed store and additive Supabase RPC migration. Focused embed **18/18**, nonce **19/19**, and semantic/docs **37/37** passed; authored volumes were regenerated. Fresh independent security acceptance remains active, and the migration/credential/browser/provider/deployment gates remain external. No network/live/cloud/provider/database/migration apply/deploy/push/main action occurred.
- `ERASURE-002-INDEPENDENT-P2-AND-CLASSIFICATION-CORRECTION` — fresh read-only review then local documentation correction — 2026-09-12 — reviewer found **P0 none, P1 none, P2 one**: the race/trigger/RPC boundary is sound, but `(agency_id, client_id, erased_at)` was overstated as PII-free. The authored database chapter and semantic registry now truthfully classify the retained opaque client id as pseudonymous personal data: contact-data-free and minimal, but access-restricted and subject to retention review. The earlier evidence line is historical and superseded only on this classification point. Re-acceptance is pending; no network/live/cloud/provider/database/migration apply/deploy/push/main action occurred.
- `PUBLIC-WEBHOOK-GENERIC-ERROR-FIRST-REACCEPTANCE` — fresh read-only review + hermetic probes — 2026-09-12 — **REJECT, P0 none, P1 none, P2 two**. Finance classified missing API configuration as non-retryable 400 while equivalent missing webhook secrets returned 503, and Memberships did not distinguish absent webhook capability from invalid signatures. The shared logger also accepted mutable `Error.name` as a category. Existing generic malformed-body envelopes, exact signed bytes, 256 KiB boundaries, no-store and response non-reflection held; focused **8/8**, adjacent **61/61**, TypeScript and diff passed. No external action occurred.
- `PUBLIC-WEBHOOK-GENERIC-ERROR-SECOND-CORRECTION` — local source correction + hermetic regression — 2026-09-12 — Finance now resolves configuration before signature verification; Memberships exposes a narrow exact-scope webhook-capability check and rechecks after failed verification; all five facades return retryable `503 webhook_unavailable` for missing configuration before body access and non-retryable `400 webhook_refused` for missing/invalid signatures. Logger provider/phase values and failure categories are fixed unions; raw Error objects/name/message/stack never reach it. Focused **5/5**, expanded adjacent **97/97**, full TypeScript and diff passed. Fresh read-only final acceptance is active. No network/live/cloud/provider/database/migration/deploy/push/main action occurred.
- `ERASURE-002-FINAL-INDEPENDENT-ACCEPTANCE` — fresh read-only source/schema/docs review + hermetic regression — 2026-09-12 — **ACCEPT, P0 none, P1 none, P2 none**. PostgreSQL lock/trigger ordering closes writer-first and erasure-first identity-link races; exact composite scope prevents cross-tenant false positives; direct table/function access is revoked; exceptions roll back tombstone plus deletion; repeat/no-row erasure is safe. The corrected registry, authored database chapter and regenerated canonical volume classify the opaque client id as pseudonymous personal data with access and retention controls. Functional client-erasure/schema/semantic **64/64**, independent static assertions **16/16**, classification recheck **12/12**, semantic/consolidation **7/7**, TypeScript and diff passed across the two review rounds. Disposable-PostgreSQL concurrency and applying the additive migration remain release gates. No edits/network/live/cloud/provider/database/migration apply/deploy/push/main action occurred in acceptance.
- `GITHUB-CHECKPOINT-001-LOCAL` — 2026-09-12 11:27 BST — the frozen recovered integration state was committed as explicitly non-release branch `checkpoint/full-build-20260912-112701` at exact SHA `3abd629ec87d43e4d5138af9204060df73e92bbc`. The commit contains 282 files, 24,473 insertions and 3,731 deletions. `git diff --cached --check` passed after two trailing-blank-line fixes. Staged-only inspection found zero files over 5 MiB, zero symlinks, zero generated/local-state paths, zero private-key/known-provider-token additions; six sensitive-name hits were confirmed as JSX `onToken` props. Secret fields in `.env.example` are blank. This is preservation evidence, not build, security acceptance or production readiness.
- `GITHUB-CHECKPOINT-001-BUNDLE` — 2026-09-12 11:27 BST — `git bundle verify` accepted `aquaCRM-orchestration/github-checkpoints/aquacrm-all-refs-20260912-112701.bundle`; it contains 56 refs and complete history, size 45 MB, SHA-256 `a54c8e1763eeec99cf93bbf1f86302124b2f7ec351beaf526093b6670b29ba9d`. Direct GitHub push was blocked before transfer because the exact remote ownership/destination was not established through trusted user input. No remote ref or `main` changed.
- `PLUGIN-LINEAGE-001-FINAL-INDEPENDENT-ACCEPTANCE` — 2026-09-12 — **ACCEPT at exact commit `a437a6055d1bf4e5524efd0b39328b7a46a0bfa0`, P0/P1/P2 none**. Applicable matrix **215/215** passed; the independent hostile matrix rejected 398 malformed/type/enum/bounds cases and accepted 28 valid/null-clear cases across installed/distributed copies. Late-invalid patches preserved storage, revision, indexes, activity and events exactly; accessors/inherited getters were not executed; immutable lineage and trusted in-process enqueue/erasure held. TypeScript, diff and installed/template byte-parity passed. One unrelated pre-existing generated Email Sender SMTP smoke syntax error remains outside this commit. Combined-tree reconciliation and rerun remain separate.
- `LOGIN-UX-001-CORRECTIVE-HANDOFF` — 2026-09-12 — clean isolated commit `52f85e2d0f6dbcdf3d42426f88e388b47c3c7f6c` corrects CAPTCHA narrow-width containment, auth-page skip targets and reset tenant identity. Implementer evidence: loopback Playwright **54/54**, focused **94/94**, TypeScript and diff green, clean worktree, no graph artifacts. Two pre-existing in-process login suites hung before registering subtests and remain explicitly pending; independent acceptance is required.
- `SOP-WORKFLOW-LINKS-001-DESIGN-DECISION` — 2026-09-12 — Ed selected canonical dynamic SOP references instead of hardcoded cold-call scripts and duplicated workflow documents. Direct source confirms the existing SOP engine already supports searchable written/file/interactive records, categories/tags, composed guides and tenant-safe reference validation; current Sales surfaces carry no SOP reference. The implementation gate will reuse that system with an in-context search/open/create picker and activity-time SOP revision evidence. This is architecture/queue truth only, not implementation or acceptance.
- `BRAND-ERASURE-001-PART-1-REVIEW` — 2026-09-12 — Claude commit `f2573d2425872ee0c1efd765fc648ffef9de0455` is an incomplete foundation, not an acceptance candidate. Its own commit truth says the new pure operations and service-role RPCs are not wired into `clientErasure` or any live metadata writer. Combined-tree inspection also finds the proposed `20260912150000_brand_enquiry_atomic_metadata.sql` collides with accepted checkpoint migration `20260912150000_durable_auth_nonces.sql`. It remains isolated; no migration was applied and no live service was contacted. Completion must uniquely renumber the migration and prove the mounted concurrent writer/erasure flow before review.
- `GITHUB-CHECKPOINT-001-REMOTE` — 2026-09-12 — after Ed explicitly authorised `https://github.com/edstorm987/aquacrm.git`, the frozen recovery branch was pushed as `checkpoint/full-build-20260912-112701`; fresh `git ls-remote` returned exact SHA `3abd629ec87d43e4d5138af9204060df73e92bbc`. Nine committed/scanned worker histories were published under `checkpoint/workers/*`; fresh remote enumeration matched every local SHA. The accepted plugin-lineage correction was reconciled without regressing newer Postmark/webhook controls and checkpointed as `checkpoint/integration/full-build-20260912` at exact remote SHA `43aa898d541fc9174089e54e3a9dab60de42c4f8`. Its combined focused gates are 14/14 lineage, 23/23 API host, 11/11 dispatcher, 30/30 tenancy and 43/43 email/Postmark; diff green. `main` was not moved and no deployment/provider/database/migration apply occurred.
- `LOGIN-UX-001-FINAL-INDEPENDENT-ACCEPTANCE` — 2026-09-12 — **ACCEPT at exact commit `52f85e2d0f6dbcdf3d42426f88e388b47c3c7f6c`, P0/P1/P2 attributable none**. The real widget is compact and fully contained at 320px, flexible at 390/768/1440, survives dynamic resize, auth pages expose working focusable skip targets, and reset preserves exact tenant/canonical return context. Committed browser **54/54**, independent functional browser **82**, auth/privacy/recovery **136/136**, login/challenge **68/68**, TypeScript and diff passed with zero external requests/page errors. Earlier in-process hangs did not reproduce and were sandbox/listener interference. Separate inherited P2 truth: OAuth-visible divider text is 2.61:1 and Aqua public-site link 4.43:1; both predate the correction and remain queued under `LOGIN-AA-001`. External Cloudflare/non-Chromium/literal UI zoom/OAuth/MFA/email journeys remain untested.
- `BRAND-ERASURE-001-PART-1-INDEPENDENT-REJECTION` — 2026-09-12 — exact commit `f2573d2425872ee0c1efd765fc648ffef9de0455` is **REJECTED as integration/release closure, P0 0/P1 6/P2 2**. The proposed RPC sets `name=NULL` against the canonical NOT-NULL column and would roll back; it deletes obsolete `replies`/`calls` while real `inboxReplies`/`inboxCalls`, recording pointers and `clientLinkSource` survive; all 13 direct RMW sites plus `clientErasure` remain unwired; completion and Aqua Tag claim/complete paths can restore lineage after erasure; client-wide discovery is not atomically fenced; its migration version collides with durable auth nonces; the helper accepts unsafe lineage and shallow merges lose same-key arrays. Narrow 5/5 and legacy erasure 54/54 are false-insufficient. A production-shaped probe retained real PII/lineage. Correction requires a durable erasure fence checked by every writer, specialised atomic operations, real metadata/blob disposition, schema-compatible anonymisation, exact actor/tenant/grant validation, atomic client-wide selection and a mounted/DB-shaped full race matrix. No live action or migration apply occurred.
- `LOGIN-AA-001-CORRECTION` — 2026-09-12 — separate exact commit `d804243305c19d57e3ded6d2573593b11b24bc40` corrects only the two inherited shared auth-theme contrast failures. Aqua accent is 4.73:1 on white and 4.61:1 on the card; OAuth divider is 5.90:1 and 5.76:1. Permanent WCAG calculation coverage is added. Axe at 320/390/768/1440 reports zero findings; independent responsive/focus/resize/reduced-motion checks **86**, permanent browser **54/54**, focused login UX **11/11**, TypeScript and diff pass, with zero external requests/page errors. GitHub worker checkpoint `checkpoint/workers/login-ux-correction-20260912` was fast-forwarded and verified at the exact SHA. Combined-tree regression remains.
- `SEC-006-THIRD-CORRECTIVE-HANDOFF` — 2026-09-12 — combined continuation commit `2439e1b59a1442eed73b7d017098e42e2a0e2e40` changes exactly 27 SEC/compliance/docs files and leaves the worktree clean. Implementer evidence is focused **18/18**, documentation/semantic **69/69**, TypeScript and diff green. Permanent cases cover stale/missing root owner and nested-scope veto, no-leak projections, realistic Person facets/history, finance ledger, real pluginData/install joins, lazy sidecar classification, 10k/100k linear work, output cap, non-fulfilment before review+delivery, exact replay and prep/delivery rollback. This is VERIFY handoff, not acceptance. One unrelated integrated-tree assertion still pins 164 non-plugin routes while 165 now exist and is queued separately.
- `ROUTE-INVENTORY-001-CORRECTION` — 2026-09-12 — isolated exact commit `e3d1df1c9917c4c84b93d348cbf29fcc26e55626` changes only `smoke-app-route-tenancy.test.ts`. The added 165th non-plugin route is `portal/settings/embed-credentials`: direct review confirms owner/manager auth, agency exclusively from session, preflight and in-transaction same-agency client checks, exact same-Origin and signed CSRF for mutation. The inventory now detects `parsed.value.clientId` and pins this route in the individually classified client-input set instead of falsely marking it public. Route tenancy/auth/embed matrix **41/41**, TypeScript and diff pass. The correction is accepted for later integration. Its scanned GitHub worker checkpoint `checkpoint/workers/route-inventory-20260912` was independently verified at exact remote SHA `e3d1df1c9917c4c84b93d348cbf29fcc26e55626`; remote `main` remained `d932ce665d9f6afaffa75ed7796a69b17ac7ee58`. No live/provider/database/migration/deployment action occurred.
- `SEC-006-THIRD-INDEPENDENT-REJECTION` — 2026-09-12 — exact combined-tree SHA `2439e1b59a1442eed73b7d017098e42e2a0e2e40` is **REJECTED, P0 0/P1 5/P2 1 attributable to SEC-006**. Direct hostile probes proved equivalent UK/E.164 phones and typed nested owners can be misclassified as exclusive; allowlisted source/link/external-reference strings can export third-party PII and bank identifiers; known SubjectRequest lifecycle/evidence fields are silently omitted with zero omission count and a false automatic-complete label; the exact successful delivery replay returns `request_not_ready`; 50k foreign-tenant rows consume the subject's bound and unmetered nested owner accumulation becomes super-linear; POST/PUT/PATCH omit the project signed-CSRF control. Focused SEC still passed 18/18, proving that matrix false-insufficient; adjacent total was 139 pass/1 pre-existing route-count failure, TypeScript and diff green. The separate route-count P2 is already corrected at `e3d1df1c`. Reviewer made no edits/network/live actions. Full report: `reviews/SEC-006-2439e1b5.md`.
- `GITHUB-ACCEPTED-INTEGRATION-LINE` — 2026-09-12 — because independent review rejected SEC-006 after its candidate was checkpointed, no remote ref was rewritten. `checkpoint/integration/full-build-20260912` remains frozen historical evidence at `2439e1b5`. A new accepted-only branch, `checkpoint/integration/accepted-full-build-20260912`, was created from and independently verified at exact last accepted combined SHA `43aa898d541fc9174089e54e3a9dab60de42c4f8`. Remote `main` was reverified unchanged at `d932ce665d9f6afaffa75ed7796a69b17ac7ee58`. Route/login may advance the accepted line only after exact integration/regression; no force push, deployment or live mutation occurred.
- `GITHUB-ORCHESTRATION-CHECKPOINT` — 2026-09-12 — the current three-lane operating pack was copied into `docs/roadmaps/2026-09-12-full-build` on an isolated non-release branch. It contains 17 Markdown files / 937 lines covering Codex and Claude prompts, decisions, queue, feature map, human blockers, Git policy, evidence and independent review reports. Staged diff, private-key/provider-token scan, large-file and symlink checks were clean. Remote `checkpoint/orchestration/full-build-20260912` was independently verified at exact SHA `fce64ab005fd93d085ac6d708e8245cd2a0cb7da`; remote `main` remained `d932ce665d9f6afaffa75ed7796a69b17ac7ee58`. This is preservation, not production acceptance.
- `LOGIN-ROUTE-INTEGRATION-001-HANDOFF` — 2026-09-12 — fresh candidate `d8dd30bbdddf47b7d9ecf7a1fdfb683faecf9cf8` is based on accepted parent `43aa898d`, then applies accepted route inventory `e3d1df1c` and the accepted login/contrast chain through `d8042433`. The exact delta is ten route/login-owned files, with the login conflict resolved to keep the single-card UI plus newer exact `clientId`, tenant-brand, CAPTCHA, OAuth and MFA behavior. Implementer evidence: combined **209/209**, Chromium responsive/focus **54/54**, dynamic CAPTCHA resize and axe 3/3 auth routes; diff green and worktree clean. Full TypeScript remains blocked by known missing `fulfilSubjectAccessRequest` in the accepted parent's not-yet-corrected SEC area, which this candidate does not touch. Independent exact-SHA acceptance is active; this is not yet an accepted-line or `main` update.
- `LOGIN-ROUTE-INTEGRATION-001-INDEPENDENT-REJECTION` — 2026-09-12 — exact SHA `d8dd30bbdddf47b7d9ecf7a1fdfb683faecf9cf8` is **REJECTED P0 0/P1 2/P2 2**. A candidate-introduced global skip handler fails to move keyboard focus on at least 15 non-auth `#main-content` roots; inherited integration gaps discard exact `clientId` through showcase→live, use static/unbound branding in recovery, and discard validated tenant/client intent through OAuth state/callback/errors. Positive evidence remains strong: focused login/route 27/27, bot challenge 27/27, Chromium 54/54, 18 independent auth route/viewport scans with zero serious/critical axe findings or external requests/errors, resize/reduced-motion/diff green. The combined tree also retains one unchanged SEC TypeScript error. No source/live action by reviewer. Full report: `reviews/LOGIN-ROUTE-INTEGRATION-001-d8dd30bb.md`.
- `SEC-006-PASS4-CORRECTIVE-HANDOFF` — 2026-09-12 — isolated exact commit `59520ecd15b041a4afde6b88cb945ed789561026` changes six SEC-owned files and reports canonical phone ownership, safe bounded nested typed claims, Set-based accumulation, tenant-local/full work accounting, PII/banking redaction with exact counters, exhaustive SubjectRequest field classification, durable exact delivery replay and signed CSRF. Implementer evidence: focused **24/24**, adjacent excluding the separately accepted route pin **146/146**, TypeScript and diff green, clean worktree. Broad 161/162 fails only the known 164→165 route assertion corrected at `e3d1df1c`. Exact-SHA hostile independent review is active; this is not acceptance.
- `ORCHESTRATION-DOCS-001-INDEPENDENT-REJECTION` — 2026-09-12 — first GitHub orchestration snapshot `fce64ab005fd93d085ac6d708e8245cd2a0cb7da` is **REJECTED for documentation truth, P0 0/P1 6/P2 3**: conflicting Git authority, rejected ref called accepted, conflicting worker assignments, stale mixed-era current status, snapshot-vs-canonical ambiguity, stale feature order, historical plugin labels and `LIVE` wording. No secrets, emails, customer PII or private keys were found; prompt bodies were all below 4,000 bytes. The external canonical pack was corrected before any replacement snapshot can be accepted. Full report: `reviews/ORCHESTRATION-DOCS-001-fce64ab0.md`.
