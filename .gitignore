# dependencies
node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# macOS
.DS_Store

# testing
coverage

# next.js — including per-worker isolated build dirs (.next-verify, .next-alpha, ...)
.next
.next-*
# Supervisor-owned preview state. Unanchored so it also matches inside an
# isolated preview worktree, which is a checkout of this same repository.
.aqua-preview-config/
# Isolated per-project preview worktrees (git worktrees on aqua-editor/<projectId>).
.aqua-preview-worktrees/
out

# production
build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env.*
!.env.example
!.env.local.example
.env*.local

# vercel
.vercel

# supabase local state
supabase/.temp
supabase/.branches
supabase/.env

# typescript
*.tsbuildinfo
next-env.d.ts

# local file-backend state
.data

# Generated customer projects are independent repositories.
/client-projects/

# R8 milesymedia stitch — public/_milesy/ is a build artefact copied
# from `04-the-final-portal/milesymedia website/` by
# `scripts/prepare-milesy.mjs`. Source lives in the static-site
# folder; this is the materialised mirror Next.js serves.
public/_milesy/

# local-only private deployment/docs handoff
Dev Team/
